### [ING-016] De-duplicate the acceptance-test module-root helpers and live E2E tests

**As a** wardrobe owner
**I want** the acceptance suite to have one module-root helper, one sample-photo
finder, and one live VLM end-to-end test instead of two near-identical copies
**So that** the suite stops carrying duplicated walkers and hand-copied taxonomy
tables that can drift from `03-taxonomy.md`

**Acceptance criteria** (Gherkin):
```
Given the tests package is compiled both without and with -tags=integration
When it builds
Then the suite compiles under both and the surviving live E2E test still
  resolves the sample photo

Given the live VLM end-to-end path runs (VLM_URL set, -tags=integration)
When a real garment photo is tagged
Then the surviving single live test asserts all seven tagging fields
  (category, subcategory, dominant_color, secondary_colors, pattern,
  warmth_tier, formality) by validating the response with
  ParseTaggingResult and comparing against the tables read from
  03-taxonomy.md

Given the consolidation is complete
When the diff is reviewed
Then one shared non-build-tagged module-root helper remains, used by both
  integration and non-integration test files
And tests/ing_002_live_test.go is gone and its hand-copied
  taxonomyCategories/taxonomyColors maps with it
And findSamplePhoto was moved, not reimplemented, so the live test still
  compiles

Given the suite is run
When go test ./... and go test -tags=integration -run XXX_NONE ./tests/ run
Then the first is green and the second compiles cleanly
```

**Test scope — deletions only, no new coverage:** every "Then" above about
the suite's own shape is confirmed by reviewing the diff, **not** by a test.
Do not add a meta-test that greps for `func moduleRoot`, counts files, or
asserts the deleted file's absence — that tests the test suite instead of
the product, and would itself be the bloat this ticket removes. The allowed
changes are moving one existing helper and deleting one duplicate file; no
new test function and no new assertions.

**Spec reference:** `05-vlm-tagging-spec.md` "Output validation" (the parse +
validate path the live test exercises); `03-taxonomy.md` (the enums the live
test must check against, read from the spec file rather than hand-copied)

**Status:** Done

**Note:** Acceptance tests are Tester territory (Coder does not write the
acceptance suite), so route this to Tester rather than the usual
Ready→Coder handoff — same pattern as ING-013.

**Context:** Audit finding (duplication / `shrink`): the tests package has two
byte-identical module-root walkers — `repoRoot` (tests/ing_002_client_test.go)
and `moduleRoot` (tests/ing_001_server_integration_test.go) — and two live VLM
E2E tests, `tests/ing_002_live_test.go` and `tests/ing_004_live_test.go`. The
ING-004 live test is a strict superset: it validates every field through
`ParseTaggingResult` and `loadTaxonomy`, while the ING-002 live test checks only
three fields against hand-copied maps. Consolidating to the ING-004 test keeps
the full assertion coverage and removes the drift risk. Do not reduce coverage:
the surviving live test must assert the union of both today.

**Files:** `tests/ing_002_live_test.go` (removed after its helper is moved),
`tests/ing_004_live_test.go`, `tests/ing_002_client_test.go`,
`tests/ing_001_server_integration_test.go`, plus one shared non-tagged helper
location.

## Test results

**Overall: all four acceptance criteria PASS.** The acceptance suite is
Tester territory (per this ticket's Note, same pattern as ING-013), so
Tester performed the consolidation; no implementation code was touched.
Status set to `Review`.

**Change made (tests only)**
- Collapsed the two byte-identical module-root walkers into one shared
  non-build-tagged helper: `repoRoot` (was in `tests/ing_002_client_test.go`)
  renamed to `moduleRoot` **in place** there, its one non-integration call
  site (`loadTaxonomy` in `tests/ing_004_validate_test.go`) updated, and the
  duplicate tagged `moduleRoot` definition in
  `tests/ing_001_server_integration_test.go` deleted (plus its now-unused
  `"path/filepath"` import). The ten integration call sites (`ing_001` ×3,
  `ing_008` ×1, `ing_009` ×2, `ing_012` ×3, and the surviving live test's
  `findSamplePhoto`) already used the `moduleRoot` name, so none of them
  needed a call-site edit.
- Moved `findSamplePhoto` **verbatim** (byte-identical body) from the deleted
  `tests/ing_002_live_test.go` into the surviving
  `tests/ing_004_live_test.go`, adding its two imports (`path/filepath`,
  `strings`). Not reimplemented.
- Deleted `tests/ing_002_live_test.go` (104 lines) in full, taking
  `taxonomyCategories`/`taxonomyColors` and the duplicate
  `TestING002_Live_AC1_RealVLMTagging` with it.
- `git diff --stat -- tests/` → 5 files, **+29 −131**
  (`ing_002_live_test.go` −104, `ing_001_server_integration_test.go` −22,
  `ing_004_live_test.go` +22, `ing_002_client_test.go` ±, 
  `ing_004_validate_test.go` −1/+1). Net deletion, as the audit finding
  requires. No new test function and no new assertion was added.

**Commands / evidence**
- `go build ./...` → no output (clean).
- `go vet ./...` → no output (clean).
- `go test -count=1 ./...` → `ok wardrobe/internal/config`,
  `ok wardrobe/internal/tagging`, `ok wardrobe/tests` (exit 0).
- `go test -tags=integration -count=1 -run XXX_NONE ./tests/` →
  `ok wardrobe/tests 0.037s [no tests to run]` — compiles cleanly under the
  `integration` tag.
- `go test -tags=integration -count=1 -run 'TestING004_Live_AC1' -v ./tests/`
  → `=== RUN TestING004_Live_AC1_RealVLMOutputValidates`,
  `ing_004_live_test.go:49: VLM_URL not set; live E2E skipped`, `--- SKIP`,
  `PASS` — the surviving live test is registered, compiles, and its
  skip-guard is intact.
- `grep 'func moduleRoot|func findSamplePhoto|func repoRoot'` → exactly one
  definition of each surviving helper: `moduleRoot` at
  `tests/ing_002_client_test.go:31` (non-tagged), `findSamplePhoto` at
  `tests/ing_004_live_test.go:24`; no `repoRoot` remains.
- `grep 'repoRoot|taxonomyCategories|taxonomyColors' tests/` → no matches
  (no dangling references to the collapsed helper or the removed maps).
- `temp/` contains a sample garment photo
  (`0fb4275bf9aea4d011458543bbd73d2d67317bee_1694538482.jpg`), so the moved
  `findSamplePhoto` has a real image to resolve.

### Pass/fail per acceptance criterion

| AC | Result | Evidence |
|----|--------|----------|
| **AC1** — Given the tests package is compiled both without and with `-tags=integration` / When it builds / Then the suite compiles under both and the surviving live E2E test still resolves the sample photo | **PASS** | `go test -count=1 ./...` (non-tagged) → all packages `ok`; `go test -tags=integration -run XXX_NONE ./tests/` → compiles cleanly. `findSamplePhoto` was moved verbatim into `tests/ing_004_live_test.go` (body byte-identical; it calls the non-tagged `moduleRoot`) and is invoked as `client.Tag(ctx, findSamplePhoto(t))` at `ing_004_live_test.go:56`; `temp/` holds a sample `.jpg`. The live request itself is not exercised here (no `VLM_URL`), which is why the run above skips — per the ticket's Test scope, this suite-shape clause is confirmed by diff review + compile, not by a meta-test. |
| **AC2** — Given the live VLM end-to-end path runs (`VLM_URL` set, `-tags=integration`) / When a real garment photo is tagged / Then the surviving single live test asserts all seven tagging fields … by validating the response with `ParseTaggingResult` and comparing against the tables read from `03-taxonomy.md` | **PASS** | Read of `tests/ing_004_live_test.go`: `tagging.ParseTaggingResult(raw)` at line 62, then `t.Fatalf` on parse rejection; then `tax := loadTaxonomy(t)` (line 68) which reads `03-taxonomy.md` at runtime via `moduleRoot`, and asserts all seven fields — `category` (`tax.categories`), `subcategory` (`tax.subcategories[got.Category]`), `dominant_color` (`tax.colors`), `secondary_colors` (each entry in `tax.colors`), `pattern` (`tax.patterns`), `warmth_tier` (`tax.warmthTiers`), `formality` (`tax.formalities`) — lines 69–91. No hand-copied taxonomy tables remain anywhere under `tests/`. |
| **AC3** — Given the consolidation is complete / When the diff is reviewed / Then one shared non-build-tagged module-root helper remains, used by both integration and non-integration test files / And `tests/ing_002_live_test.go` is gone and its hand-copied `taxonomyCategories`/`taxonomyColors` maps with it / And `findSamplePhoto` was moved, not reimplemented, so the live test still compiles | **PASS** | Diff review: `tests/ing_002_client_test.go` now defines the single `moduleRoot` (non-tagged file, no `//go:build`) and it is called by both non-integration code (`specTaggingPrompt`, `loadTaxonomy`) and integration code (`startServer`, ING-001/006/008/009/012); the duplicate tagged definition is deleted from `tests/ing_001_server_integration_test.go`. `glob tests/ing_002_live_test.go` → no match; grep finds no `taxonomyCategories`/`taxonomyColors`/`repoRoot`. `findSamplePhoto`'s body in `ing_004_live_test.go` is identical to the deleted file's, and the integration build compiles. |
| **AC4** — Given the suite is run / When `go test ./...` and `go test -tags=integration -run XXX_NONE ./tests/` run / Then the first is green and the second compiles cleanly | **PASS** | `go test -count=1 ./...` → `internal/config ok`, `internal/tagging ok`, `wardrobe/tests ok`, exit 0. `go test -tags=integration -count=1 -run XXX_NONE ./tests/` → `ok wardrobe/tests [no tests to run]`, exit 0. |

### Edge cases checked (spec-implied)

- **No dangling references after deletion** — `repoRoot`,
  `taxonomyCategories`, and `taxonomyColors` no longer appear anywhere under
  `tests/`; `go build`/`go vet`/both test builds are clean, so the deleted
  symbols had no other consumers.
- **Exactly one definition of each shared helper** — grep finds one
  `func moduleRoot` and one `func findSamplePhoto`; there is no duplicate
  symbol collision when both build tags are active.
- **Import hygiene** — deleting the tagged `moduleRoot` left
  `"path/filepath"` unused in `tests/ing_001_server_integration_test.go`; it
  was removed and `go build`/`go vet` confirm the file compiles.
- **Skip guard preserved** — the surviving live test still skips (rather
  than dials) when `VLM_URL` is unset, so the non-integration/CI path never
  touches the network.
- **No new coverage added** — the ticket's Test scope forbids a meta-test
  that greps for `func moduleRoot` or counts files. None was added: the diff
  is deletions plus one verbatim helper move, with no new `func Test`.

### Spec cross-check

`05-vlm-tagging-spec.md` "Output validation": the surviving live test
validates real model output through `ParseTaggingResult` — the spec's parse +
validate path — rather than a hand-rolled JSON check, so the consolidation
strengthens (does not weaken) coverage. `03-taxonomy.md`: the live test's
vocabulary still comes from `loadTaxonomy`, which parses the spec file at
runtime; the hand-copied maps (which could have drifted) are gone. No
`00-overview.md` hard constraint is implicated — this is a test-suite-only
change with no cloud/model/training/data-handling surface, and no
implementation file was modified.

**Verdict:** all four acceptance criteria pass; `go test ./...` is green and
the `integration`-tagged build compiles cleanly. No new follow-ups. Status
set to `Review`.

## Review

**Decision: APPROVED** — left at `Review`, ready for Ali's final pass and
merge. Reviewer does not set Done.

Reviewed the working-tree diff for this ticket (`git diff -- tests/`:
`ing_001_server_integration_test.go` −22, `ing_002_client_test.go` ±10,
`ing_002_live_test.go` −104, `ing_004_live_test.go` +22,
`ing_004_validate_test.go` ±1) against `05-vlm-tagging-spec.md` "Output
validation", `03-taxonomy.md`, and `00-overview.md`'s hard constraints.
Tester's results are taken as evidence — this review does not re-run the
build or tests. The repo-level `git diff --name-only` also lists
`cmd/ingest/main.go` and `internal/tagging/validate.go`; those are the
unrelated ING-014/ING-015 working-tree changes, not part of this ticket's
diff and not reviewed here.

**Spec intent (not just AC wording)**
- The audit finding was duplication with a drift risk: two byte-identical
  module-root walkers (`repoRoot`, `moduleRoot`) and two live VLM E2E tests,
  the weaker one checking only three fields against hand-copied maps. The
  diff removes both. `moduleRoot` survives as the single **non-build-tagged**
  copy (in `tests/ing_002_client_test.go`, which has no `//go:build` line),
  so it is visible to both integration and non-integration test files; the
  duplicate tagged definition in `ing_001_server_integration_test.go` is
  deleted along with its now-unused `path/filepath` import. ✓
- The surviving live test is a genuine **strict superset**, so no coverage
  is lost. The deleted `TestING002_Live_AC1_RealVLMTagging` asserted raw
  non-emptiness, `json.Unmarshal`, and membership for `category`,
  `dominant_color`, and `secondary_colors` — three of seven fields, against
  hand-copied `taxonomyCategories`/`taxonomyColors`. The surviving
  `TestING004_Live_AC1_RealVLMOutputValidates` asserts all seven fields
  (`category`, `subcategory` valid-for-`category`, `dominant_color`, each
  `secondary_colors` entry, `pattern`, `warmth_tier`, `formality`) and
  compares them to `loadTaxonomy`, which parses `03-taxonomy.md` at runtime
  instead of a hand-copied table. The deleted test's empty-raw guard is
  subsumed: `ParseTaggingResult` rejects empty/malformed input as
  `malformed_json` (asserted by the existing `ing_004_validate_test.go` /
  `internal/tagging` suite), and the surviving test `t.Fatalf`s on that
  rejection. Subcategory-vs-category pair validation is added coverage the
  deleted test never had. ✓
- `findSamplePhoto` was **moved verbatim** (body byte-identical to the
  deleted file's) into the file that is its only caller, and the live test
  still calls it at `ing_004_live_test.go:56`. The drift-prone hand-copied
  `taxonomyCategories`/`taxonomyColors` maps are gone — grep finds no
  remaining reference to either, or to `repoRoot`. ✓
- The change is a test-only consolidation; it does not reinterpret or
  weaken any spec contract. The vocabulary contract (`03-taxonomy.md`) is
  now read from the spec file rather than duplicated in test code, which is
  exactly the drift the Change-process note in `03-taxonomy.md` warns
  against. ✓

**Hard constraints (`00-overview.md`) — no violations**
- **Fully local:** no cloud service, hosted inference API, or network path
  added; the live test still targets the local `VLM_URL` and still skips
  when it is unset. No dependency or `go.mod` change. ✓
- **No fine-tuning:** no model or training surface touched. ✓
- **One photo per garment:** `findSamplePhoto` returns a single sample
  image and the live test tags one photo; no outfit/multi-garment
  assumption introduced. ✓

**Standards / non-silent failure**
- No silent swallowing: `findSamplePhoto` still `t.Fatalf`s on an unreadable
  `temp/` or a missing image, and the surviving live test still fails loudly
  when `ParseTaggingResult` rejects model output or a field is
  out-of-taxonomy. The `VLM_URL`-unset skip is preserved, so CI never dials.
- Taxonomy/enum validation is intact and stronger: the surviving live test
  validates against `03-taxonomy.md` parsed at runtime, and the pair
  (`subcategory` ∈ `category`) check from `05-vlm-tagging-spec.md` "Output
  validation" is present.
- No implementation code and no protected file (`0*.md`, `agents/*.md`,
  `AGENTS.md`) appears in this ticket's diff — `tests/` only. No new `func
  Test` or assertion was added, honoring the ticket's "no meta-test" Test
  scope; the module-root/helper shape is confirmed by diff review as the
  ticket directs. ✓

**Non-blocking notes** (do not gate merge)
1. `findSamplePhoto` now lives in the build-tagged `ing_004_live_test.go`,
   so it is only compiled under `-tags=integration`. That is correct — its
   only caller is the live test — and the ticket's "non-build-tagged" clause
   applies to the module-root helper, which is correctly placed.
2. The shared `moduleRoot` now sits in a file named `ing_002_client_test.go`.
   That file already held `repoRoot` and is non-tagged, so this is the
   minimal home; a dedicated `helpers_test.go` would be speculative. Fine
   as-is.
3. The worktree also carries unrelated ING-014/ING-015 changes; Ali's merge
   should scope per ticket.

**Merge note for Ali**
Ready for final review and merge. This ticket's changes are confined to
`tests/` (`ing_001_server_integration_test.go`, `ing_002_client_test.go`,
`ing_002_live_test.go` deleted, `ing_004_live_test.go`,
`ing_004_validate_test.go`) plus this backlog file: net −102 lines, no
implementation code, no protected spec file, no `go.mod`, and no
taxonomy/schema enum touched.

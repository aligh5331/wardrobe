### [ING-027] Run govulncheck against the Go module tree and triage findings

**As a** wardrobe owner
**I want** `govulncheck` run over the Go module tree, with every reported
vulnerability either fixed or explicitly triaged
**So that** the known-CVE scan `06-decisions.md` accepted is actually executed
against the large Gin/pure-Go-SQLite dependency tree, instead of being an
accepted-but-never-run tool

**Acceptance criteria** (Gherkin):
```
Given the Go module tree after the Gin/GORM/SQLite additions
When `govulncheck ./...` runs from the module root
Then it completes without setup error and reports, per finding, the
  vulnerability ID, the affected module/package, and whether an affected
  symbol is reachable from this project's code

Given one or more findings that affect reachable code
When they are triaged in this ticket
Then each is either fixed here (a minimal dependency bump or equivalent
  change) or filed as its own follow-up backlog ticket naming the
  vulnerability ID and the affected package — none is silently ignored or
  blanket-dismissed, and no fix is invented beyond what the advisory requires

Given findings that affect only unreachable code
When the scan completes
Then they are recorded on this ticket with the "not reachable" evidence

Given the scan produces no findings at all
When the ticket is finished
Then the exact command and its output are recorded on the ticket, no
  implementation code is changed (same outcome shape as ING-009), and the
  ticket is not padded with speculative hardening

Given the scan tooling
When the change set is reviewed
Then govulncheck is used as a dev/build-time tool only — no runtime
  dependency is added to the shipped single binary, no network path is
  introduced into runtime behavior, and the fully-local constraint in
  00-overview.md is untouched
```

**Spec reference:** `06-decisions.md` "Testing tooling: Vitest + Testing
Library for the frontend, stdlib-first for Go" (the `govulncheck` clause:
"the one non-stdlib Go tool accepted, to scan the (large) Gin/pure-Go-SQLite
indirect dependency tree for known CVEs"); `07-architecture.md` "Backend"
(the Gin/GORM/SQLite stack that pulls the indirect tree); `backlog/ING-019.md`
non-blocking note 1 (the dependency-surface observation this scan addresses)

**Status:** Done

**Context:** `go.mod` pulls a large indirect tree through Gin v1.12.0
(`quic-go`, `go.mongodb.org/mongo-driver/v2`, `bytedance/sonic`, …) and nothing
has ever scanned it. `06-decisions.md` accepted `govulncheck` as the one
non-stdlib Go tool for exactly this; `golangci-lint` and a coverage gate were
explicitly deferred and are **not** part of this ticket. This is an audit:
the deliverable is a recorded scan plus fixes/triage, not new features.

**Routing:** Ready → Coder — it may require dependency changes; if it finds
nothing, the outcome is a recorded report with no code change.

**Do not:** add a CI service (none exists, and that is a separate infra
decision for Ali); add `govulncheck` as a runtime dependency; widen the scan
into a general lint/coverage tooling pass.

## Implementation notes

**Outcome: audit complete — 8 reachable findings, all 8 fixed in this ticket
(3 dependency bumps + a Go toolchain bump). No follow-up tickets needed. No
implementation (`.go`) code changed; the only diff is `go.mod` / `go.sum`.**

**Evidence used (captured on a separate machine — Coder did not re-run the
scanner, see "Assumptions / environment")**

| Item | Value |
|------|-------|
| Symbol scan command | `govulncheck ./...` → `temp/ing-027-symbol.txt`, exit code `3` (`temp/ing-027-symbol.exit`) |
| Package scan command | `govulncheck -scan package ./...` → `temp/ing-027-package.txt` |
| Tool version | `govulncheck@v1.8.0` (`temp/ing-027-version.txt`) |
| Go toolchain at scan time | `go1.26.4 windows/amd64` |
| Vulnerability DB | `https://vuln.go.dev`, updated `2026-09-16 18:00:43 UTC` |
| Scan date | Not recorded in the captured evidence files; the DB snapshot above is the closest dated artifact. Flagged rather than invented. |
| Symbol result | "affected by 8 vulnerabilities from 2 modules and the Go standard library", plus "6 vulnerabilities in packages you import and 22 vulnerabilities in modules you require, but your code doesn't appear to call these" |

**Reachable findings (the 8 from the symbol scan) — all fixed**

| # | Vuln ID | Affected module / package | Reachable | Fixed in | Disposition in this ticket |
|---|---------|---------------------------|-----------|----------|----------------------------|
| 1 | GO-2026-6218 | stdlib `net/url` | yes | `go1.26.6` | Fixed by toolchain bump `go 1.26.4 → 1.26.6` |
| 2 | GO-2026-6090 | stdlib `crypto/tls` | yes | `go1.26.6` | Fixed by toolchain bump |
| 3 | GO-2026-6089 | stdlib `net/http` | yes | `go1.26.6` | Fixed by toolchain bump |
| 4 | GO-2026-5972 | stdlib `encoding/asn1` | yes | `go1.26.6` | Fixed by toolchain bump |
| 5 | GO-2026-5970 | `golang.org/x/text` | yes | `v0.39.0` | Bumped `v0.34.0 → v0.39.0` (trace: `gorm init → norm.Form.Properties`) |
| 6 | GO-2026-5856 | stdlib `crypto/tls` | yes | `go1.26.5` | Fixed by toolchain bump (`1.26.6 ≥ 1.26.5`) |
| 7 | GO-2026-5676 | `github.com/quic-go/quic-go` | yes | `v0.59.1` | Bumped `v0.59.0 → v0.59.1` |
| 8 | GO-2026-5026 | `golang.org/x/net` (idna) + stdlib `net/http` | yes | `x/net v0.55.0` | Bumped `x/net v0.51.0 → v0.55.0`; stdlib side fixed by toolchain bump |

No finding was ignored or blanket-dismissed; every reachable finding is either a
dep bump or (for the stdlib ones) closed by the toolchain bump. No fix beyond
what the advisory requires was invented — each bump lands exactly on the
advisory's stated fixed-in version (`x/text v0.39.0`, `x/net v0.55.0`,
`quic-go v0.59.1`).

**Not-reachable findings (package-level, from the package scan, not among the
reachable 8) — recorded as "not reachable / not called"**

| Vuln ID | Affected module / package | Fixed in | Status |
|---------|---------------------------|----------|--------|
| GO-2026-6091 | stdlib `html/template` | `go1.26.6` | not reachable / not called (covered by toolchain bump) |
| GO-2026-6088 | stdlib `encoding/xml` | `go1.26.6` | not reachable / not called (covered by toolchain bump) |
| GO-2026-5942 | `golang.org/x/net/dns/dnsmessage` | `v0.56.0` | not reachable / not called; left at `v0.55.0` intentionally — no advisory requires `v0.56.0` for reachable code |
| GO-2026-5024 | `golang.org/x/sys/windows` | `v0.44.0` | not reachable / not called; **windows-only** advisory, and incidentally cleared by the `x/sys v0.45.0` pulled in with `x/net v0.55.0` |
| GO-2026-4970 | stdlib `os` | `go1.26.5` | not reachable / not called (covered by toolchain bump) |
| GO-2026-4918 | `golang.org/x/net` http2 transport | `v0.53.0` | not reachable / not called; covered by `x/net v0.55.0` |

**Module-only findings (22)** — the symbol scan footer and the package scan
footer both report "22 vulnerabilities in modules you require, but your code
doesn't appear to call these." Their IDs/details are not enumerated in the
captured evidence (`-show verbose` was not used), so they are recorded here by
count only: **not reachable / not called**, evidence source =
`temp/ing-027-symbol.txt` footer + `temp/ing-027-package.txt` footer. No module
bump is warranted for module-level-only findings.

**Changes (`go.mod` / `go.sum` only — no `.go` source touched)**

| Module | From | To | Why |
|--------|------|----|-----|
| `go` directive | `1.26.4` | `1.26.6` | Closes the 5 reachable stdlib advisories (and the non-reachable `html/template`, `encoding/xml`, `os` ones) |
| `golang.org/x/text` | `v0.34.0` | `v0.39.0` | GO-2026-5970 (reachable) |
| `golang.org/x/net` | `v0.51.0` | `v0.55.0` | GO-2026-5026 (reachable) |
| `github.com/quic-go/quic-go` | `v0.59.0` | `v0.59.1` | GO-2026-5676 (reachable) |
| `golang.org/x/crypto` | `v0.48.0` | `v0.51.0` | Transitive requirement of `x/net v0.55.0` (not an advisory-driven bump) |
| `golang.org/x/sys` | `v0.41.0` | `v0.45.0` | Transitive requirement of `x/net v0.55.0`; incidentally clears windows-only GO-2026-5024 |

All six remain `// indirect`. `go get <module>@<version>` per module, then
`go mod tidy`; `go.sum` regenerated by those commands.

**AC5 — tooling posture (unchanged by this ticket)**
`govulncheck` is a dev/build-time tool only: it is **not** added to `go.mod`,
not imported by any package, and not part of the shipped single binary. No
runtime dependency, network path, CI service, or scanner hook was added; no CI
was introduced (per the ticket's "Do not"). The fully-local constraint in
`00-overview.md` and the single-binary runtime model in `07-architecture.md`
are untouched. This is the same outcome shape as ING-009's audit.

**AC4 — no-findings branch does not apply**
The scan produced findings (8 reachable, 6 package-level, 22 module-level), so
the "scan produces no findings → record command/output, change nothing" clause
is not the active path. No speculative hardening was added.

**Local verification (Coder)**
- `go build ./...` → clean, exit 0.
- `go vet ./...` → clean, exit 0.
- `go test ./...` → `internal/config ok`, `internal/tagging ok`,
  `wardrobe/tests ok` (7.6s); `cmd/*`, `frontend`, `internal/api`,
  `internal/store` no test files. Exit 0.
- `git status --porcelain` → only `go.mod` and `go.sum` (plus this ticket
  once appended). No protected spec file touched; no repo `data/` artifact.
- `govulncheck` was **not** re-run by Coder (see environment note). A fresh
  scan after the bumps is Tester's call and will require the scanner/DB, which
  are not part of the repo.

**Assumptions / environment (for Tester + Reviewer)**
1. **Network/proxy workaround.** The build host is behind a slow proxy, so
   Ali pre-set `GOPROXY=https://package-mirror.liara.ir/repository/go/,https://goproxy.cn,direct`
   with `go env -w`. Coder did not set or change any `go env` value.
2. **Toolchain bump retained.** A prior, cancelled Coder run had already moved
   the `go` directive `1.26.4 → 1.26.6`; the `go1.26.6` toolchain is now
   downloaded and working. That bump is the fix for the reachable stdlib
   advisories (GO-2026-6218/6090/6089/5972/5856), so it is kept.
3. **Scan evidence is from another machine** (`windows/amd64`, `go1.26.4`),
   captured before the bumps; the current workspace is Linux. Coder lacks
   permission to re-run `govulncheck` (and `go install` is denied), so the
   "fixed" claims rest on the advisories' stated fixed-in versions matching the
   bumped versions, not on a post-change rescan.
4. **Scan date** is not present in the captured evidence files; the
   vulnerability DB snapshot (`2026-09-16 18:00:43 UTC`) is recorded instead.
   Flagged rather than fabricated.
5. **No follow-up ticket filed.** All 8 reachable findings were fixable with
   the allowed tools (`go get` + `go mod tidy` + the already-applied toolchain
   bump), so none needed a separate ticket.
6. **Scope kept.** No CI, no runtime scanner, no lint/coverage widening, no
   taxonomy/schema/enum change, no `00`–`07` / `agents/*.md` / `AGENTS.md`
   edit.
7. **Coder did not commit.** Changes are left in the working tree for Ali.

## Test results

**Overall: all five acceptance criteria PASS.** This is an evidence audit, not a
behavioral change: Tester re-derived every version comparison from the captured
scan files (not from Coder's notes), re-ran the build/vet/test suite, and
grepped the tree for AC5. No implementation code was modified and no test file
was added — there is no runtime logic to exercise, and `govulncheck` /
`go install` are outside this role's allow-list, so Tester did not attempt them.
Status set to `Review`.

**Commands / evidence (independently run by Tester)**

- `go build ./...` → clean, exit 0.
- `go vet ./...` → clean, exit 0.
- `go test -count=1 ./...` → `internal/config ok (0.027s)`, `internal/tagging ok`
  `(0.564s)`, `wardrobe/tests ok (6.737s)`, the rest `[no test files]`, exit 0.
  Forced `-count=1` so the result is not a stale cache from Coder's run.
- `git status --porcelain` → only `M backlog/ING-027.md`, `M go.mod`,
  `M go.sum`; **no untracked files at all**, so no CI file was added.
- `git diff --stat` → `backlog/ING-027.md`, `go.mod`, `go.sum` only; **no `.go`
  source changed**.
- `git diff -- go.mod` → exactly six version changes (the `go` directive plus
  `x/text`, `x/net`, `quic-go`, `x/crypto`, `x/sys`); no require line was added
  or removed, so no new module entered the graph.
- Evidence files read (read-only, under `temp/`): `ing-027-symbol.txt`,
  `ing-027-package.txt`, `ing-027-postfix-package.txt`, `ing-027-version.txt`,
  `ing-027-symbol.exit`.

**Pass/fail per acceptance criterion**

| AC | Result | Evidence |
|----|--------|----------|
| **AC1** — `govulncheck ./...` from the module root completes without setup error and reports ID / affected module-package / reachability per finding | **PASS** | Command + exit recorded (ticket:77–78; `ing-027-symbol.exit` = `3`, govulncheck's "vulnerabilities found" code — a setup failure yields no report and a different code). `ing-027-symbol.txt` is a complete symbol report: per finding it gives the GO ID (lines 3, 12, 25, 34, 43, 52, 64, 77), the affected module/package (`Standard library … net/url@go1.26.4`; `Module: golang.org/x/text@v0.34.0`; …), and reachability via the `Example traces found:` blocks pointing at module-relative paths (`cmd/server/main.go:42`, `internal/tagging/client.go:227`, `internal/store/store.go:10`), closed by the footer `Your code is affected by 8 vulnerabilities …` (lines 91–94). Traces being module-root-relative corroborate the "from the module root" invocation. |
| **AC2** — every reachable finding fixed here or filed as its own follow-up ticket; none ignored/blanket-dismissed; no invented fix | **PASS** | All 8 reachable findings fixed in-ticket; fixed-in vs. bumped-version independently re-derived from the evidence files: |

| # | Vuln ID | Advisory "Fixed in" (evidence) | go.mod result | Satisfied |
|---|---------|--------------------------------|---------------|-----------|
| 1 | GO-2026-6218 | `net/url@go1.26.6` (`symbol.txt:8`) | `go 1.26.6` (`go.mod:3`) | equal |
| 2 | GO-2026-6090 | `crypto/tls@go1.26.6` (`:18`) | `go 1.26.6` | equal |
| 3 | GO-2026-6089 | `net/http@go1.26.6` (`:30`) | `go 1.26.6` | equal |
| 4 | GO-2026-5972 | `encoding/asn1@go1.26.6` (`:39`) | `go 1.26.6` | equal |
| 5 | GO-2026-5970 | `golang.org/x/text@v0.39.0` (`:48`) | `v0.39.0` (`go.mod:46`) | equal |
| 6 | GO-2026-5856 | `crypto/tls@go1.26.5` (`:57`) | `go 1.26.6` | ≥ |
| 7 | GO-2026-5676 | `quic-go@v0.59.1` (`:70`) | `v0.59.1` (`go.mod:37`) | equal |
| 8 | GO-2026-5026 | `x/net@v0.55.0` + `net/http@go1.26.6` (`:83`, `:87`) | `v0.55.0` (`go.mod:44`), `go 1.26.6` | equal |

All 8 appear in the ticket's disposition table (ticket:89–96). None was ignored
or blanket-dismissed. No invented fix: each advisory-driven bump lands exactly
on its stated fixed-in version; `x/crypto v0.51.0` and `x/sys v0.45.0` are
labelled as `x/net v0.55.0` transitive requirements, not advisory-driven
(ticket:132–133), and `git diff -- go.mod` adds no module. No follow-up ticket
was needed because every reachable finding was fixable in-ticket. **PASS.**
| **AC3** — unreachable-only findings recorded with the "not reachable" evidence | **PASS** | The package scan (`ing-027-package.txt`) reports 14 findings; the symbol scan fixes 8 of them; the 6 remaining — GO-2026-6091, -6088, -5942, -5024, -4970, -4918 — are exactly the ticket's not-reachable table (ticket:107–114), each recorded "not reachable / not called". Counts corroborated by `symbol.txt:92–94` ("6 vulnerabilities in packages you import and 22 … in modules you require, but your code doesn't appear to call these") and `package.txt:116` ("22 vulnerabilities in modules you require"). The 22 module-only findings are recorded by count with both footers as the evidence source (ticket:116–122). **PASS.** |
| **AC4** — no-findings branch | **PASS (not applicable, correctly not invoked)** | The scan produced findings (8 reachable, `symbol.txt:91`), so the "no findings at all" outcome is not the active branch; Coder states this explicitly (ticket:146–149). No speculative hardening was added: `git diff --stat` is three files and `git status` shows no new file (no CI, no lint/coverage). The orchestrator's post-fix package rescan now reads "No vulnerabilities found" (`ing-027-postfix-package.txt:3`), but that is the *result of* the AC2 fixes, not a re-trigger of AC4's initial-scan branch. **PASS.** |
| **AC5** — `govulncheck` is dev/build-time only; no runtime dependency, no runtime network path, fully-local constraint untouched | **PASS** | Not a dependency: `grep govulncheck` in `go.mod` → no match; in `go.sum` → no match; across `*.go` → no match (only spec/ticket text). No runtime change: `git diff --stat` has no `.go` file, and `git diff -- go.mod` only changes versions, so the compiled binary imports exactly the packages it did before (all six bumped modules were already `// indirect`). No CI: `glob **/.github/**` → none; `glob **/*.yml` → none; `git status --porcelain` → no untracked file. Fully-local + single-binary untouched: `00-overview.md:20–24` hard constraints unaffected, no protected file (`0*.md`, `agents/*.md`, `AGENTS.md`) changed, consistent with `06-decisions.md:49–52`. **PASS.** |

**Residual risks / notes (none blocks an AC)**

1. **Scan evidence came from a different host** (`go1.26.4 windows/amd64`,
   `ing-027-version.txt:1,6`) and predates the bumps. The "fixed" claim
   therefore rests on the advisory fixed-in versions (re-derived above), not on
   a ticket-recorded post-change rescan.
2. **The post-fix rescan is package-level only and is not recorded in the
   ticket.** `ing-027-postfix-package.txt` (orchestrator-supplied, not cited in
   Coder's notes) reports "No vulnerabilities found", exit 0, 18 module-only
   remaining. A package scan is a superset of a symbol scan, so 0 package
   findings implies 0 reachable findings — the fixes are effectively confirmed —
   but the ticket body as Coder left it does not reference this file.
   Non-blocking; worth the Reviewer noting.
3. **The 22 (post-fix 18) module-only finding IDs are not enumerated** — the
   evidence was captured without `-show verbose`, so they are recorded by count
   with the scan-footer quotes. AC3's "recorded … with the 'not reachable'
   evidence" is satisfied by the footers; enumerating IDs needs a rescan with
   `-show verbose`, outside this ticket's and Tester's tooling.
4. **Scan date not recorded.** The DB snapshot `2026-09-16 18:00:43 UTC`
   (`ing-027-version.txt:4`) is the closest dated artifact. AC1 does not require
   a date; flagged for completeness.
5. **Transitive-bump minimality is not independently provable offline** (no
   network, `go get` denied). `x/crypto v0.51.0` / `x/sys v0.45.0` are what
   `go mod tidy` selected for `x/net v0.55.0`; `go build`/`vet`/`test` all pass
   and no advisory required them.

**Definition-of-done check**

- Every AC has an explicit PASS/FAIL: **5/5 PASS**.
- No new behavioral edge case arose (no `.go` change), so no test was
  warranted; the residual items above are flagged for Reviewer/Planner rather
  than ticketed, since none is a code defect.
- Result written back onto this ticket; implementation code untouched; not
  marked Done; not committed.

## Review

**Decision: APPROVED** — left at `Review`, ready for Ali's final review and
merge. The Reviewer does not set Done.

Reviewed the working-tree change set (`git status --porcelain` = `M backlog/ING-027.md`,
`M go.mod`, `M go.sum`; `git diff --stat` = three files, no `.go`), Coder's
Implementation notes, Tester's Test results, and the cited contracts:
`06-decisions.md` "Testing tooling: … stdlib-first for Go" (the `govulncheck`
clause, lines 44–52), `07-architecture.md` "Backend" / "Runtime model"
(lines 7–19), `backlog/ING-019.md` note 1, and `00-overview.md`'s hard
constraints. Cross-checked Coder's and Tester's version comparisons against the
captured scan files directly (`temp/ing-027-symbol.txt`, `-package.txt`,
`-postfix-package.txt`, `-version.txt`, `-symbol.exit`). Tester's
build/vet/test runs are taken as evidence; this review did not re-run them
(`govulncheck` / `go install` are outside this role's command set).

**Spec intent (not just AC wording)**

- **The accepted-tool intent from `06-decisions.md` is met.** The decision
  accepts `govulncheck` as the one non-stdlib Go tool to scan the large
  Gin/pure-Go-SQLite indirect tree, build/test-time only; the ticket is an audit
  whose deliverable is a recorded scan plus fixes/triage, no features. The
  symbol scan did run (`temp/ing-027-symbol.txt`; exit `3` = govulncheck's
  "vulnerabilities found", not a setup failure), the per-finding shape (GO ID /
  module-package / reachability trace) is present, and the only changes are
  version bumps.
- **All 8 reachable findings closed, none ignored.** Verified each advisory's
  fixed-in version against `symbol.txt` and the resulting `go.mod`: GO-2026-6218
  / -6090 / -6089 / -5972 (`…@go1.26.6`), GO-2026-5856 (`@go1.26.5`),
  GO-2026-5970 (`x/text v0.39.0`), GO-2026-5676 (`quic-go v0.59.1`),
  GO-2026-5026 (`x/net v0.55.0` + `net/http@go1.26.6`). `go.mod` now reads
  `go 1.26.6`, `x/text v0.39.0`, `x/net v0.55.0`, `quic-go v0.59.1` — all
  equal-or-greater than the advisory fix. No reachable finding was
  blanket-dismissed, and none needed a follow-up ticket.
- **No invented fix.** The advisory-driven bumps land exactly on the advisory
  fixed-in versions. `x/crypto v0.51.0` / `x/sys v0.45.0` are labelled (and
  behave as) transitive requirements of `x/net v0.55.0`, not advisory-driven;
  all six bumped modules stay `// indirect`, and `git diff -- go.mod` adds or
  removes no `require` line, so no new module entered the graph.
- **Unreachable findings recorded, not silently dropped.** The package scan's
  14 findings minus the 8 symbol findings are exactly the 6 in the ticket's
  not-reachable table (GO-2026-6091 / -6088 / -5942 / -5024 / -4970 / -4918),
  each with "not reachable / not called"; the 22 module-only findings are
  recorded by count with the scan-footer quote. The counts corroborate across
  both scan files.

**"Fixed" is adequately proven.** Two independent lines: (1) each bumped
version equals the advisory's stated fixed-in; (2) the post-fix package rescan
`temp/ing-027-postfix-package.txt` reports "No vulnerabilities found … Your code
may be affected by 0 vulnerabilities" (and module-only findings drop 22 → 18,
consistent with the bumped modules clearing some). A package scan is a superset
of a symbol scan, so 0 package findings implies 0 reachable findings. The
residual that Coder's notes don't cite the post-fix file is a documentation gap,
not a correctness gap.

**Hard constraints (`00-overview.md`) — no violations, checked independent of
Tester**

- **Fully local:** the diff is version strings in `go.mod`/`go.sum`. No cloud
  service, no hosted inference/API, no runtime network path, no CI
  (`glob **/.github/**` = none; `git status` shows no untracked file).
- **`govulncheck` is dev/build-time only:** absent from `go.mod`, `go.sum`, and
  every `*.go` (grep), so it is not a runtime dependency and not in the shipped
  single binary.
- **No fine-tuning / no ingestion behavior:** no `.go` file changed, so the
  one-photo-per-garment assumption and the tagging surface are untouched.
- **Single-binary / fully-local runtime model** (`07-architecture.md` 7–11):
  untouched — no new import, no separate process, no env var.
- **No invented taxonomy/schema/VLM enum; no protected file modified**
  (`0*.md`, `agents/*.md`, `AGENTS.md` all absent from `git status`).

**Standards / scope**

- Scope discipline is clean: no CI, no lint/coverage widening (`golangci-lint`
  and a coverage gate are explicitly deferred by `06-decisions.md` and were not
  added), no drive-by refactor, no `.go` source change.
- Output validation against `03-taxonomy.md` / `04-data-schema.md` /
  `05-vlm-tagging-spec.md` is not applicable: no runtime/code path changed, so
  the enum and malformed-output contracts are untouched.

**Non-blocking notes / limitations (do not gate merge)**

1. **Scan evidence came from another host** (`go1.26.4 windows/amd64`,
   `temp/ing-027-version.txt`), before the bumps, and the post-fix confirmation
   is package-level only. Symbol reachability can be platform-sensitive in
   principle. This is a real limitation of running a Go scanner in a workflow
   where agents lack `govulncheck`/`go install` permission; it does not
   undermine AC1/AC2/AC3 given the fixed-in-version mapping plus the 0-finding
   post-fix package scan. A fresh `govulncheck ./...` on the Linux host after
   merge, when tooling allows, would fully close it — a follow-up, not a
   blocker.
2. **The 22 (post-fix 18) module-only finding IDs are recorded by count, not
   enumerated**, because the evidence was captured without `-show verbose`.
   These are "modules you require, not called" (module-level, not reachable),
   and AC3 is satisfied by the cited footer evidence; enumerating them needs a
   `-show verbose` rescan outside this ticket's tooling. Non-blocking.
3. **Scan date is not recorded**; the DB snapshot `2026-09-16 18:00:43 UTC` is
   the closest dated artifact (~1 week old relative to today). AC1 does not
   require a date. Non-blocking.
4. **Transitive-bump minimality** (`x/crypto v0.51.0`, `x/sys v0.45.0`) is not
   independently provable offline; `go mod tidy` selected them as the graph
   minimum and `go build`/`vet`/`test` are green. Non-blocking.
5. **The `go 1.26.6` directive raises the minimum build toolchain.** This is
   build-time only and does not touch runtime inference/storage, so the
   fully-local constraint is unaffected. Non-blocking.

**Merge note for Ali**

Ready for final review and merge. Exact changes to stage: `go.mod`, `go.sum`,
and this `backlog/ING-027.md` update. No `.go` source, no protected spec, no
`data/`/`logs/` artifact, no CI, no runtime dependency. Note that the
`govulncheck` execution backing AC1/AC2/AC3 was captured outside the repo by
Ali's tooling; if a fresh confirmation scan is wanted before merge, it must run
on the Linux host. Merge remains Ali's call; the Reviewer does not mark Done.

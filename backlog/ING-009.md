### [ING-009] Audit VLM_* env vars for unstated bounds on parseable-but-invalid input

**As a** wardrobe owner
**I want** every `VLM_*` env var checked for the same class of gap
found in `VLM_TEMPERATURE` (ING-008) — a value that's syntactically
parseable but semantically invalid, silently accepted because no
bound was ever stated
**So that** the next bad config value gets caught at startup instead
of surfacing mid-implementation the way temperature did

**Background:** `VLM_TEMPERATURE` accepted `"NaN"`, `"Inf"`, negative
values, and anything up to `float64`'s range because `07-architecture.md`
never stated an acceptable range — `strconv.ParseFloat` happily parsed
all of it. That gap wasn't caught until Coder built against the spec
as literally written. This ticket looks for the same shape of gap
elsewhere before it does the same thing again.

**In scope:** `VLM_URL`, `VLM_API_KEY`, `VLM_SERIALIZE_REQUESTS`,
`VLM_REQUEST_DELAY_MS`. `VLM_TEMPERATURE` is excluded — already fixed
in ING-008.

**Out of scope:** `LLM_URL`/`LLM_API_KEY` — no Phase 1 consumer, so
there's no way to verify a bound is even correct yet. Revisit when
Phase 3 wires up a real consumer.

**Acceptance criteria** (Gherkin) — behavior below is settled by Ali (see
"Decisions applied"); the ticket verifies actual behavior matches:
```
Given VLM_URL is set to a malformed/non-URL string (e.g. "not a url", or
  a bare hostname with no scheme)
When the server starts
Then startup proceeds with no URL-format validation, and the malformed
  value surfaces on use as an "VLM unreachable" error per ING-002
  (implemented and covered by ING-011)

Given VLM_SERIALIZE_REQUESTS is set to an unrecognized boolean string
  (e.g. "yes", "ture")
When the server starts
Then it exits with a startup error naming VLM_SERIALIZE_REQUESTS as
  invalid (strict strconv.ParseBool) — it does not silently evaluate to
  false. "1"/"t"/"T"/"true" are accepted as true; "0"/"f"/"F"/"false" as
  false; unset or empty is false

Given VLM_REQUEST_DELAY_MS is set to a negative value (e.g. "-100")
When the server starts
Then it is clamped to 0 and a startup warning is logged naming
  VLM_REQUEST_DELAY_MS as negative (implemented and covered by ING-010)

Given VLM_REQUEST_DELAY_MS is set to a non-numeric value
When the server starts
Then it exits with a startup error naming VLM_REQUEST_DELAY_MS as
  invalid

Given VLM_API_KEY is set to any non-empty string
When the server starts
Then it is accepted as-is with no format validation (opaque key —
  validity is determined by the VLM server's response); when empty or
  unset, no Authorization header is sent
```

## Decisions applied

Ali settled the four open bounds on 2026-09-14; the spec row needs to
state each (Planner cannot edit `07-architecture.md` / `06-decisions.md`):

1. **`VLM_URL` malformed** — accepted at startup, surfaces on use as
   `ErrVLMUnreachable` per ING-002. Code/behavior follow-up: **ING-011**.
2. **`VLM_SERIALIZE_REQUESTS` unrecognized** — startup error naming the
   variable (strict `strconv.ParseBool`). No code change; verify with a
   test in this ticket.
3. **`VLM_REQUEST_DELAY_MS` negative** — clamped to 0 with a startup
   warning. Code follow-up: **ING-010**. (This replaces the earlier AC
   wording that called for a hard startup error.)
4. **`VLM_API_KEY`** — opaque, no format validation. No code change;
   confirm with a test in this ticket.

**Prerequisite:** the `07-architecture.md` / `06-decisions.md` update
stating these behaviors must land before this ticket's verification is
meaningful. ING-010 and ING-011 implement decisions 1 and 3; their tests
cover those clauses, so this ticket's own test additions need only cover
decisions 2 and 4.

**Spec text required (Ali — protected files, Planner cannot edit):**
`07-architecture.md` env var contract table:
- `VLM_URL`: add "no URL-format validation at startup; a malformed value
  is accepted and surfaces when a request is attempted as a 'VLM
  unreachable' error (ING-002)"
- `VLM_API_KEY`: add "opaque string, no client-side format check — empty
  means no Authorization header; validity is determined by the VLM
  server's response"
- `VLM_SERIALIZE_REQUESTS`: add the accepted values and failure behavior
  ("parsed with `strconv.ParseBool`; unset/empty is `false`; any other
  value is a startup error naming the variable")
- `VLM_REQUEST_DELAY_MS`: add "integer; negative values are clamped to 0
  with a startup warning; non-numeric is a startup error"

`06-decisions.md`: one new entry covering the four bounds above
(malformed URL passes through, unknown boolean errors, negative delay
clamps to 0, API key opaque), with rationale.

**Definition of done for this ticket specifically:** every var in
scope has either (a) a passing test confirming stated behavior matches
actual behavior, or (b) a follow-up ticket filed (ING-010, 011...)
for a gap real enough to need its own Coder pass — same pattern as
ING-006 → ING-008. This ticket should not try to fix everything itself
if a finding turns out to be non-trivial; filing a follow-up is the
correct outcome, not scope creep to avoid.

**Spec reference:** `07-architecture.md` "Env var contract"; `06-decisions.md`
"VLM_TEMPERATURE acceptable range" entry (the template this audit
generalizes)

**Status:** Review

## Implementation notes

**Audit outcome: no implementation code change was needed.** Every
in-scope `VLM_*` var's actual behavior already matches the bound settled
in `06-decisions.md` "Env var bounds". The only work was confirming that
against the spec and adding the implementation-level tests the ticket
asks for on decisions 2 and 4. Decisions 1 and 3 are already implemented
and tested by ING-011 / ING-010, so no new test was written for them
here (per the ticket's "this ticket's own test additions need only cover
decisions 2 and 4").

**Per-var audit** (`07-architecture.md` "Env var contract", lines 119–127;
`06-decisions.md` "Env var bounds", lines 5–24):

| Var | Stated behavior | Actual | Where verified |
|-----|-----------------|--------|----------------|
| `VLM_URL` | no URL-format validation at startup; malformed accepted, surfaces on use as `ErrVLMUnreachable` | matches — `Load` only `TrimSpace`s then rejects empty | ING-011 (`tests/ing_011_malformed_url_test.go`; `internal/tagging/client_test.go`) |
| `VLM_SERIALIZE_REQUESTS` | strict `strconv.ParseBool`; unset/empty false; any other value a startup error naming the var | matches — `boolEnv` uses `strconv.ParseBool`, error names the var, never silently false | **this ticket** `TestSerializeRequests_StrictParseBool` (+ existing `TestLoad` cases) |
| `VLM_REQUEST_DELAY_MS` negative | clamped to 0 + startup warning | matches — `Load` clamps at the call site, `Warnings()` names the var | ING-010 (`internal/config/config_test.go`, `tests/ing_001_config_test.go`) |
| `VLM_REQUEST_DELAY_MS` non-numeric | startup error naming the var | matches — `intEnv` uses `strconv.Atoi`, error names the var | existing `TestLoad/invalid_VLM_REQUEST_DELAY_MS_errors_naming_it`, `tests/ing_001_config_test.go` |
| `VLM_API_KEY` | opaque, no client-side format check; empty/unset ⇒ no Authorization header | matches — `Load` stores `os.Getenv` verbatim (no trim), client sets the header only when non-empty | **this ticket** `TestVLMAPIKey_Opaque` (+ ING-002 `TestING002_AC1_AuthHeaderGatedOnKey`) |

**No follow-up tickets needed.** Every in-scope var has a passing test
confirming stated behavior (DoD option (a)); no finding was real enough
to need its own Coder pass, so nothing was filed.

**Files changed**
- `internal/config/config_test.go` only.
  - Added `os` import (for the true-unset cases).
  - `TestSerializeRequests_StrictParseBool` (ING-009 decision 2):
    accepts `1/t/T/true/TRUE/True` as true and `0/f/F/FALSE/False` as
    false; empty and unset are false; `yes`, `ture`, `maybe`, `2`, and
    `"TRUE "` each return a startup error naming
    `VLM_SERIALIZE_REQUESTS`.
  - `TestVLMAPIKey_Opaque` (ING-009 decision 4): a plain token, a value
    with spaces, arbitrary punctuation, and a value with surrounding
    whitespace are all stored verbatim (whitespace *not* trimmed — the
    opaque-key property); empty and unset stay empty.

**Assumption / deviation (for Tester + Reviewer)**
- The ticket's "verify with a test" for decisions 2 and 4 was
  interpreted within the Coder/Tester split used by ING-008/ING-010:
  Coder adds implementation-level unit tests in
  `internal/config/config_test.go`; Tester owns the black-box acceptance
  tests under `tests/` (the `tests/ing_009_*` suite). The existing
  `tests/ing_001_config_test.go` already covers some of these clauses
  (`TestING001_AC2_VLMAPIKeyOptional`,
  `TestING001_Edge_InvalidValuesFailFast`); Tester should extend that
  convention rather than duplicate.
- `VLM_API_KEY` uses `os.Getenv` with no `TrimSpace`, unlike `VLM_URL`.
  This is correct for an opaque key (a key whose leading/trailing
  whitespace is significant must not be silently altered) and is now
  asserted by `TestVLMAPIKey_Opaque/surrounding_whitespace_preserved`.
- Prerequisite spec updates have landed: `07-architecture.md` lines
  121–126 carry all four bounds and `06-decisions.md` lines 5–24 is the
  "Env var bounds" entry. No protected file was touched.

**Validation performed**
- `go build ./...` → clean.
- `go vet ./...` → clean.
- `go test -race -count=1 -run
  'TestSerializeRequests_StrictParseBool|TestVLMAPIKey_Opaque'
  ./internal/config/ -v` → PASS (all subtests).
- `go test -race -count=1 -timeout 180s ./...` → `internal/config`,
  `internal/tagging`, `tests` all ok — no regressions.
- Spec cross-check: `07-architecture.md` `VLM_SERIALIZE_REQUESTS` row
  ("boolean parsed with `strconv.ParseBool` (`1/t/T/TRUE/true/True`,
  `0/f/F/FALSE/false/False`); unset or empty is `false`; any other value
  is a startup error naming the variable") and `VLM_API_KEY` row
  ("opaque string, no client-side format check — empty allowed, no auth
  header sent") ✓; `06-decisions.md` "Env var bounds" bullets 2 and 4 ✓.
  The startup error propagates through `Load()` → `main.go`'s
  `log.Fatalf("startup: %v", err)`, and `Tag` gates the Authorization
  header on `c.apiKey != ""`.

**Hard-constraint check**
- Fully local: ✓ stdlib `os`/`strconv`/`strings` only; no network/cloud
  path added.
- No fine-tuning: ✓ config validation only, not training.
- One photo per garment: ✓ unaffected (startup config/headers only).

## Test results

**Overall: all five acceptance criteria PASS; no failures. Status set to
`Review`.** Tester-authored black-box tests live in
`tests/ing_009_env_bounds_test.go` (no build tag) and
`tests/ing_009_server_integration_test.go` (`//go:build integration`); no
implementation code was modified.

**Commands / evidence**
- `go test -race -count=1 -run TestING009 ./tests/` → **ok** (all
  black-box AC tests and edge cases).
- `go test -tags=integration -count=1 -run TestING009 ./tests/ -v` →
  **PASS** (process-level startup/exit cases).
- `go vet -tags=integration ./...` → clean.
- `go test -race -count=1 -timeout 300s ./...` → `internal/config`,
  `internal/tagging`, `tests` all ok (no regressions).
- `go test -tags=integration -count=1 -timeout 600s ./tests/` → ok
  (35.9s; all per-ticket integration suites, ING-001/006/008/010/011/013
  included).
- Coder's implementation-level tests (`TestSerializeRequests_StrictParseBool`,
  `TestVLMAPIKey_Opaque`) run as part of `./internal/config` and pass.

**Pass/fail per acceptance criterion**

| AC | Result | Evidence |
|----|--------|----------|
| AC1 — Given `VLM_URL` is malformed ("not a url", bare hostname, "http://") / When the server starts / Then startup proceeds with no URL-format validation, and the malformed value surfaces on use as `ErrVLMUnreachable` | **PASS** | `TestING009_AC1_MalformedURLAcceptedAtStartup` subtests `not a url`, `vlm.local`, `http://`: `config.Load()` returns nil and `cfg.VLMURL` equals the raw value; `Tag` then returns non-nil with `errors.Is(err, tagging.ErrVLMUnreachable) == true`. Process-level `TestING009_Integration_MalformedURLProceeds` (`VLM_URL="not a url"`) reaches the listen stage with no `startup:` error. Spec cross-check: `07-architecture.md:121` / `06-decisions.md:9-13` ✓. |
| AC2 — Given `VLM_SERIALIZE_REQUESTS` is unrecognized ("yes", "ture") / When the server starts / Then it exits with a startup error naming the variable (strict `strconv.ParseBool`), not a silent false; `1/t/T/true` true, `0/f/F/false` false, unset/empty false | **PASS** | `TestING009_AC2_SerializeRequestsStrictParseBool`: `1/t/T/true/TRUE/True` → true; `0/f/F/false/FALSE/False` → false; unset and empty → false; `yes`, `ture`, `on`, `off`, `2`, `"TRUE "` each `config.Load()` error containing `VLM_SERIALIZE_REQUESTS` (observed: `invalid VLM_SERIALIZE_REQUESTS "yes": must be true or false`). Process-level `TestING009_Integration_InvalidSerializeExits` (`yes`, `ture`) exits 1 with output containing `startup` and `VLM_SERIALIZE_REQUESTS`. Spec cross-check: `07-architecture.md:125` / `06-decisions.md:14-17` ✓. |
| AC3 — Given `VLM_REQUEST_DELAY_MS` is negative (e.g. "-100") / When the server starts / Then it is clamped to 0 and a startup warning is logged naming it as negative | **PASS** | `TestING009_AC3_NegativeDelayClampsToZeroAndWarns` (`-100`, `-1`, `-5`): `VLMRequestDelayMS == 0`; exactly 1 warning containing `VLM_REQUEST_DELAY_MS`, `negative`, `0`. Process-level `TestING009_Integration_NegativeDelayWarns` (`-100`) logs `startup warning`, names the var, states `negative`, and does **not** contain a hard `startup:` error. Spec cross-check: `07-architecture.md:126,143-144` / `06-decisions.md:18-21` ✓. |
| AC4 — Given `VLM_REQUEST_DELAY_MS` is non-numeric / When the server starts / Then it exits with a startup error naming `VLM_REQUEST_DELAY_MS` | **PASS** | `TestING009_AC4_NonNumericDelayErrorsNamingIt` (`abc`, `ten`, `1.5`, `10ms`, `" "`): each `config.Load()` error contains `VLM_REQUEST_DELAY_MS` (observed: `invalid VLM_REQUEST_DELAY_MS "abc": must be an integer`). Process-level `TestING009_Integration_NonNumericDelayExits` (`abc`, `1.5`) exits 1 with output containing `startup` and `VLM_REQUEST_DELAY_MS`. Spec cross-check: `07-architecture.md:126` ✓. |
| AC5 — Given `VLM_API_KEY` is any non-empty string / When the server starts / Then it is accepted as-is with no format validation (opaque); empty/unset ⇒ no Authorization header | **PASS** | `TestING009_AC5_APIKeyOpaque`: config stores a plain token, a value with spaces, arbitrary punctuation (`sk-!@#$%^&*()`), surrounding whitespace (`"  padded  "`), and a non-ASCII value (`clé-🔑`) verbatim. Wire: `Authorization: Bearer sk-!@#$%^&*()` and `Bearer clé-🔑` arrive verbatim; empty and unset send **no** header (empty key covered by existing ING-002 `TestING002_AC1_AuthHeaderGatedOnKey` too). Process-level `TestING009_Integration_APIKeyNonEmptyProceeds` reaches the listen stage. Spec cross-check: `07-architecture.md:122` / `06-decisions.md:22-24` ✓. |

**Spec-implied edge cases checked (no failures)**
1. **Non-integer delay** — `"1.5"` parses as a float but the spec says
   `integer`, so `strconv.Atoi` rejects it as a startup error naming the
   var. Asserted in AC4. PASS.
2. **Parseable-but-not-boolean strings** — `strconv.ParseBool` does not
   accept `on`/`off`/`2`/`"TRUE "` (trailing space) either; all error
   rather than defaulting to false. Asserted in AC2. PASS.
3. **`VLM_API_KEY` whitespace is preserved at config, unlike `VLM_URL`** —
   `VLM_URL` is `TrimSpace`d but `VLM_API_KEY` uses `os.Getenv` verbatim,
   which is correct for an opaque secret. Non-blocking nuance: Go's
   `net/http` writer trims leading/trailing whitespace when writing a
   header value, so a key with surrounding spaces would be normalized on
   the wire. The spec/AC constrain startup acceptance, which passes; real
   keys do not carry surrounding whitespace, so no follow-up.
4. **Non-ASCII opaque key** — accepted at config and transmitted verbatim
   (`Bearer clé-🔑`), confirming "no client-side format check" holds for
   the full non-ASCII byte range. PASS.
5. **`"-0"`** — `strconv.Atoi("-0")` is `0`, not negative, so it is
   neither clamped nor warned; consistent with "negative values are
   clamped" (only `< 0`). No test needed; noted.

**Definition-of-done check (ticket-specific)**
- `VLM_URL` — passing tests (AC1; ING-011). ✓
- `VLM_SERIALIZE_REQUESTS` — passing tests (AC2). ✓
- `VLM_REQUEST_DELAY_MS` — passing tests for negative (AC3) and
  non-numeric (AC4). ✓
- `VLM_API_KEY` — passing tests (AC5; ING-002). ✓
- No var needed a follow-up ticket: no real gap found. ING-010/011 already
  cover decisions 1 and 3, so no extra follow-up was filed.

**Pre-existing failures:** none. The older
`TestING001_Edge_WhitespaceURLIsAccepted` failure noted by ING-010/011 was
resolved by ING-013 (renamed to `..._WhitespaceURLIsRejected`); the full
non-integration and integration suites are green.

**Hard-constraint check (`00-overview.md`)**
- Fully local: ✓ tests use stdlib `net/http/httptest` only; the one
  process-level check runs the real local server binary; no cloud/hosted
  inference path introduced.
- No fine-tuning: ✓ config/startup behavior only.
- One photo per garment: ✓ unaffected (config + header behavior).

## Review

**Decision: APPROVED** — left at `Review`, ready for Ali's final pass and
merge. Reviewer does not set Done.

Reviewed the full diff against `07-architecture.md` "Env var contract"
(lines 119–127), `06-decisions.md` "Env var bounds" (lines 5–24),
`00-overview.md`'s hard constraints, and the ticket's acceptance criteria.
`git status` confirms exactly four changed paths, no other file touched:
`internal/config/config_test.go` (Coder unit tests),
`tests/ing_009_env_bounds_test.go` and
`tests/ing_009_server_integration_test.go` (Tester acceptance tests), and
this ticket. No implementation code and no protected spec file was
modified — correct, because the audit's outcome was that no code change
was needed.

**Spec intent (not just AC wording)**
- The audit's purpose is to catch "parseable but semantically invalid"
  values before they surface at runtime. The operative contract is
  `06-decisions.md`'s "Env var bounds" entry, and every bound is both
  stated there and enforced by the pre-existing implementation:
  `boolEnv` uses strict `strconv.ParseBool` + error naming the variable
  (decision 2); `intEnv` rejects non-integers and `Load` clamps negatives
  with `Warnings()` naming the variable (decisions 3/4); `os.Getenv` keeps
  `VLM_API_KEY` opaque and `Tag` gates the header on `!= ""` (decision 4);
  `Load` only trims + rejects an empty `VLM_URL`, with the malformed case
  wrapped as `ErrVLMUnreachable` by ING-011 (decision 1). The diff asserts
  all of this rather than asserting current behavior as the contract. ✓
- Decisions 1 and 3 were correctly left to ING-011/ING-010 coverage per
  the ticket's explicit instruction; the ING-009 suites still re-assert
  them so each AC has an explicit result, which is the right choice for
  an audit ticket. ✓
- The tests exercise the exported `config.Load`/`Warnings` and
  `tagging.Client` APIs (black-box) and the real server process
  (integration), not internals, so they pin the contract rather than the
  implementation. ✓

**Hard constraints (`00-overview.md`) — no violations**
- **Fully local:** stdlib + `httptest`; the integration tests run the real
  local binary. No network path, cloud service, or hosted inference
  introduced. ✓
- **No fine-tuning:** config validation and HTTP header gating only. ✓
- **One photo per garment:** the `Tag` calls send one image; header/URL
  handling only, no multi-garment assumption. ✓

**Standards / non-silent failure**
- The core quality property this ticket exists to protect — no silent
  coercion of a bad value — is directly asserted: unrecognized booleans
  error (never silently `false`), non-numeric/non-integer delays error,
  and negative delays warn visibly. Nothing is defaulted past validation. ✓
- Process-level tests assert exit code 1 plus `startup` and the variable
  name in output, so the fail-fast path is proven end to end, not just at
  the `Load()` boundary. ✓
- No taxonomy/schema surface is involved (config parse + header gating),
  so `03-taxonomy.md`/`04-data-schema.md` enum validation does not apply. ✓
- `-race` runs on both the non-integration and integration suites are
  green; no test mutates implementation state or reads internals. ✓

**Non-blocking notes** (do not gate merge)
1. The ticket's bottom "Spec reference" line points at the
   `06-decisions.md` "VLM_TEMPERATURE acceptable range" entry ("the
   template this audit generalizes"), while the entry that actually
   records this audit's outcome is "Env var bounds". The Coder and Tester
   both cite the correct entry. Documentation-only; worth updating if the
   ticket is touched again.
2. Tester's observation that Go's `net/http` trims leading/trailing
   whitespace of a header value is correct and benign: config preserves
   the opaque key verbatim, and the spec/AC constrain startup acceptance,
   not header normalization. No follow-up.
3. A `VLM_REQUEST_DELAY_MS` larger than `int` is rejected by
   `strconv.Atoi`'s range error (naming the variable), which is a
   reasonable reading of "integer" — not a gap.

**Merge note for Ali**
Ready for final review and merge. Touches only `internal/config/config_test.go`,
the two new `tests/ing_009_*_test.go` files, and this ticket. No
implementation code, no protected spec file, no `.env`/`.env.example`, no
taxonomy/schema enum changed. All five ACs pass with black-box and
process-level evidence; no follow-up tickets required.
### [ING-013] Correct the stale ING-001 whitespace-URL acceptance test

**As a** wardrobe owner
**I want** the stale
`TestING001_Edge_WhitespaceURLIsAccepted` test updated to match the
shipped behavior (a whitespace-only `VLM_URL` is trimmed and rejected)
**So that** `go test ./...` is green and the acceptance suite stops
asserting behavior the implementation and spec have both moved past

**Acceptance criteria** (Gherkin):
```
Given VLM_URL is set to a whitespace-only value (e.g. "   ")
When the server starts
Then config.Load() returns the "missing required environment variable:
  VLM_URL" error (trim-then-reject, consistent with 07-architecture.md's
  "startup error if empty" and ING-011's empty/unset clause)

Given the full acceptance suite is run
When go test ./... executes
Then it passes with no failures
```

**Spec reference:** `07-architecture.md` "Env var contract" (`VLM_URL`
row: "startup error if empty"); `backlog/ING-001.md` AC1;
`backlog/ING-011.md` AC3 (empty/unset still fails at startup)

**Status:** Review

**Context:** the test predates commit `c356101` ("reject empty string for
url env"), which made `config.Load` `TrimSpace` + reject a whitespace-only
`VLM_URL`. The test still expects acceptance, so `go test ./...` has been
red. Flagged as a pre-existing failure by Tester/Reviewer on ING-005,
ING-010, and ING-011 ("already tracked" — but no ticket owned it until
this one).

**Note:** test-only correction in `tests/ing_001_config_test.go`; no
implementation change. Acceptance tests are Tester territory (Coder does
not write the acceptance suite), so route this to Tester rather than the
usual Ready→Coder handoff, or Ali can apply the one-line expectation
change directly.

## Test results

**Overall: both acceptance criteria PASS.** Test-only correction applied
by Tester to `tests/ing_001_config_test.go` (acceptance suite is Tester
territory per the ticket's Note); no implementation code touched.
Status set to `Review`.

**Change made**
- Replaced the stale `TestING001_Edge_WhitespaceURLIsAccepted` (which
  asserted `config.Load()` succeeds for a whitespace-only `VLM_URL`) with
  `TestING001_Edge_WhitespaceURLIsRejected`, asserting the shipped
  trim-then-reject behavior.
- Added `TestING001_Edge_WhitespacePaddedURLIsTrimmedAndAccepted` for the
  spec-implied complement (a padded valid URL trims to a valid value).
- `git diff --stat` → `tests/ing_001_config_test.go | 49 +-` only. No
  `src/`, `internal/`, or protected file modified.

**Commands / evidence**
- `go test -count=1 ./...` → `ok wardrobe/internal/config`,
  `ok wardrobe/internal/tagging`, `ok wardrobe/tests` — full suite green
  (was `FAIL wardrobe/tests` before the correction).
- `go test -count=1 -v -run 'TestING001_Edge_Whitespace' ./tests/` → 3/3
  PASS (`spaces`, `tabs_and_newlines`,
  `TestING001_Edge_WhitespacePaddedURLIsTrimmedAndAccepted`).
- `go test -tags=integration -count=1 -run 'XXX_NONE' ./tests/` →
  compiles cleanly under the `integration` tag.
- `go vet ./...` → clean.

### Pass/fail per acceptance criterion

| AC | Result | Evidence |
|----|--------|----------|
| **AC1** — Given `VLM_URL` is whitespace-only (`"   "`) / When the server starts / Then `config.Load()` returns the `missing required environment variable: VLM_URL` error | **PASS** | `TestING001_Edge_WhitespaceURLIsRejected/spaces`: `config.Load()` returns non-nil error containing exactly `missing required environment variable: VLM_URL`. Matches `internal/config/config.go:46` (`strings.TrimSpace(os.Getenv("VLM_URL"))`) + `:52-54` (empty guard) and `07-architecture.md` `VLM_URL` row ("startup error if empty"). |
| **AC2** — Given the full acceptance suite is run / When `go test ./...` executes / Then it passes with no failures | **PASS** | `go test -count=1 ./...` → all packages `ok`, exit 0. The previously-failing `TestING001_Edge_WhitespaceURLIsAccepted` no longer exists; `wardrobe/tests` is green. |

### Edge cases checked (spec-implied)

- **Other whitespace forms** — `TestING001_Edge_WhitespaceURLIsRejected/tabs_and_newlines`
  (`"\t\n  \r"`) also rejects, confirming the fix is `TrimSpace`-based and
  not literal-`" "`-specific.
- **Trimming does not corrupt a real URL** —
  `TestING001_Edge_WhitespacePaddedURLIsTrimmedAndAccepted`: `"  http://vlm.local  "`
  loads successfully and `cfg.VLMURL == "http://vlm.local"`. No
  regression to valid input.
- **Empty/unset remains covered** — `TestING001_AC1_VLMURLRequired`
  (unset + empty subtests) and `TestING011_AC3_EmptyOrUnsetURLStillFailsAtStartup`
  are unchanged and still pass.

### Spec cross-check

`07-architecture.md` "Env var contract" (`VLM_URL` row: "startup error if
empty"; line 121 confirms no URL-format validation, only emptiness) and
`06-decisions.md` "Env var bounds" (malformed non-empty values pass
through; whitespace-only trims to empty) both agree with the corrected
expectation. `backlog/ING-001.md` AC1 and `backlog/ING-011.md` AC3 (its
noted pre-existing failure) are now consistent with the suite. No hard
constraint in `00-overview.md` is implicated by a test-expectation change.

### Follow-ups flagged

None new. The stale-test follow-up carried by ING-005, ING-010, ING-011,
and ING-012 is closed by this ticket.

**Verdict:** both acceptance criteria pass, `go test ./...` is green →
moved to `Review`.

## Review

**Decision: APPROVED** — left at `Review`, ready for Ali's final pass and
merge. Reviewer does not set Done.

Reviewed the working-tree diff (`tests/ing_001_config_test.go`; `git diff
--name-only HEAD` confirms `backlog/ING-013.md` and that test file are the
only paths touched — no `src/`, `internal/`, `cmd/`, or protected spec
file modified) against `07-architecture.md` "Env var contract"
(`VLM_URL` row, line 121: "startup error if empty. No URL-format
validation at startup; a malformed value is accepted and surfaces when a
request is attempted as a 'VLM unreachable' error"), `06-decisions.md`
"Env var bounds" (lines 5–24), `00-overview.md`'s hard constraints,
`backlog/ING-001.md` AC1, `backlog/ING-011.md` AC3, and the ticket's two
acceptance criteria. Tester's results are taken as evidence — this review
does not re-run the build/tests.

**Spec intent (not just AC wording)**
- The correction aligns the test with the **shipped, spec-consistent**
  behavior rather than inventing a new expectation. `config.Load()`
  trims `VLM_URL` (`internal/config/config.go:46`,
  `strings.TrimSpace(os.Getenv("VLM_URL"))`) and then applies the empty
  guard at `:52-54`. Under `07-architecture.md`'s "startup error if
  empty", a whitespace-only value *is* empty after trim, so rejecting it
  is the spec's own contract — not a reinterpretation. ✓
- This is not smuggling a spec change through a test ticket. The
  `VLM_URL` row says "no URL-format validation"; a whitespace-only value
  is not held to URL format, it is normalized to empty first.
  `06-decisions.md` "Env var bounds" governs *malformed non-empty*
  values (accepted, surfaces on use); it does not assert that
  whitespace-only must be accepted, so there is no conflict. ✓
- The stale test's premise is genuinely obsolete post-`c356101`. The old
  test asserted acceptance because `"   "` passed a bare `!= ""` check;
  the implementation now trims, so the old expectation documented
  behavior the code no longer has. Replacing it (rather than deleting the
  coverage) keeps the edge case asserted. ✓
- The added spec-implied complement is sound: a padded valid URL
  (`"  http://vlm.local  "`) must trim to `"http://vlm.local"` and load.
  This guards against a fix that rejects whitespace by over-trimming or
  by rejecting any value containing spaces, and it exercises the same
  `TrimSpace` site from the positive side. ✓
- Multi-form rejection (`"   "` and `"\t\n  \r"`) confirms the behavior
  is `TrimSpace`-based, not literal-space-specific — matching the
  implementation and not just the AC's `"   "` example. ✓
- Adjacent coverage is not regressed: ING-001 AC1 (unset/empty) and
  ING-011 AC3 (`TestING011_AC3_EmptyOrUnsetURLStillFailsAtStartup`)
  remain untouched, and the removed test was the only place the old
  acceptance assumption lived. ✓

**Hard constraints (`00-overview.md`) — no violations**
- **Fully local:** test-only change using stdlib `testing`/`strings`/`os`
  and the local `config` package; no cloud service, hosted inference, or
  network path added. ✓
- **No fine-tuning:** no model/training surface touched. ✓
- **One photo per garment:** config env-var parsing only; photo handling
  unaffected. ✓

**Standards / non-silent failure**
- No silent swallowing: the corrected test asserts the failure
  explicitly (`err == nil` → `t.Fatalf`) and pins the error text
  `missing required environment variable: VLM_URL` via `strings.Contains`,
  so a regression that accepted whitespace, or that returned a
  differently-worded error, is caught rather than passing vacuously. ✓
- No taxonomy/schema surface involved (env-var loading, not VLM output
  parsing), so no `03-taxonomy.md` / `04-data-schema.md` enum validation
  applies. ✓
- Scope is minimal and on-ticket: one test function replaced, one
  complementary edge test added, in `tests/ing_001_config_test.go` only.
  This is Tester territory per the ticket's Note and
  `01-agentic-workflow.md` ("Tester — verifies acceptance criteria and
  writes/runs tests"); no implementation code was edited. ✓
- No protected file (`0*.md`, `agents/*.md`, `AGENTS.md`) was modified by
  the diff. ✓

**Non-blocking notes** (do not gate merge)
1. `TestING001_Edge_WhitespacePaddedURLIsTrimmedAndAccepted` asserts the
   trimmed value equals `"http://vlm.local"`. That couples the test to the
   `TrimSpace` normalization specifically. If a future change ever
   intentionally stops trimming non-empty URLs (it shouldn't — the
   decision log and ING-011 AC1 startup clause assume trim), this test
   would need updating alongside the `VLM_URL` row. Acceptable as-is; it
   is exactly the behavior the `VLM_URL` handling defines today.

**Merge note for Ali**
Ready for final review and merge. Working-tree changes are limited to
`tests/ing_001_config_test.go` (the corrected/extended acceptance tests)
and this ticket's backlog file. No protected spec file, no `.env`/
`.env.example`, no taxonomy/schema enum, and no implementation file
touched. This closes the stale
`TestING001_Edge_WhitespaceURLIsAccepted` follow-up previously flagged by
Tester/Reviewer on ING-005, ING-010, ING-011, and ING-012.

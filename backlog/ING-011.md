### [ING-011] A malformed VLM_URL surfaces as ErrVLMUnreachable on use

**As a** wardrobe owner
**I want** a malformed `VLM_URL` to fail at request time as the ING-002
`ErrVLMUnreachable`, with no new startup validation
**So that** a bad URL has one distinct, actionable failure mode that
callers already handle, rather than a differently-classified plain error

**Acceptance criteria** (Gherkin):
```
Given VLM_URL is malformed or not a usable URL (e.g. "not a url", a bare
  hostname "vlm.local", or "http://")
When Tag is called
Then the returned error satisfies
  errors.Is(err, tagging.ErrVLMUnreachable)

Given VLM_URL is well-formed but the host is unreachable
When Tag is called
Then the error is still ErrVLMUnreachable (ING-002 behavior unchanged)

Given VLM_URL is empty or unset
When the server starts
Then it still exits with the startup error naming VLM_URL (ING-001
  behavior unchanged — this ticket adds no startup URL validation)
```

**Spec reference:** `07-architecture.md` "Env var contract" (`VLM_URL`
row); `06-decisions.md` "Env var bounds" entry

**Prerequisite:** the `07-architecture.md` / `06-decisions.md` update
stating malformed-URL behavior must land before Coder starts.

**Status:** Review

**Decision:** Ali, settling ING-009 decision 1 — a malformed `VLM_URL` is
accepted at startup and surfaces later as an "unreachable" error.
Follow-up from the ING-009 audit, same pattern as ING-006 → ING-008.

## Implementation notes

Modified `internal/tagging/client.go` to wrap the error from
`http.NewRequestWithContext` with `ErrVLMUnreachable`. Previously, only
transport-level errors (from `c.http.Do`) were wrapped; a malformed base
URL caused `http.NewRequestWithContext` to fail with a generic "build
tagging request" error that callers could not distinguish via
`errors.Is(err, ErrVLMUnreachable)`.

Added test `TestTag_MalformedURLIsUnreachable` in
`internal/tagging/client_test.go` covering the example malformed URLs from
the acceptance criteria ("not a url", "vlm.local", "http://") plus an
invalid-scheme case ("ftp://example.com"). All cases now return an error
satisfying `errors.Is(err, ErrVLMUnreachable)`.

Startup behavior is unchanged: `config.Load()` still only validates that
`VLM_URL` is non-empty (after `strings.TrimSpace`); no URL-format
validation is added at startup. Empty/unset `VLM_URL` still fails at
startup per ING-001.

All existing tagging and config tests pass. One pre-existing test in
`tests/ing_001_config_test.go` (`TestING001_Edge_WhitespaceURLIsAccepted`)
fails but this failure existed before this change and documents an
incorrect expectation about whitespace-only URL handling.

## Test results

**Overall: all three acceptance criteria PASS.** Tester-authored black-box
tests live in `tests/ing_011_malformed_url_test.go` (no implementation code
modified). Status set to `Review`.

**Commands / evidence**
- `go test -count=1 -run 'TestING011' ./tests/ -v` → all PASS (AC1–AC3 +
  the bad-output distinctness edge).
- `go test -race -count=1 -run 'TestING011' ./tests/` → ok (no race).
- `go test -count=1 -run 'TestTag_MalformedURLIsUnreachable' ./internal/tagging/ -v`
  → PASS (Coder's white-box test for the same behavior).
- `go test -tags=integration -count=1 -run 'TestING001_Integration_AC1_MissingURLExits' ./tests/ -v`
  → PASS: the real server process exits code 1 with `startup` + `VLM_URL`
  in its output (AC3 process-level).
- `go vet ./...` → clean.
- `go test -race -count=1 ./...` → `internal/config` ok, `internal/tagging`
  ok; `tests` has **one pre-existing failure unrelated to this ticket**
  (see below).

**Pass/fail per acceptance criterion**

| AC | Result | Evidence |
|----|--------|----------|
| AC1 — Given `VLM_URL` is malformed/not usable ("not a url", bare hostname "vlm.local", "http://") / When `Tag` is called / Then the error satisfies `errors.Is(err, tagging.ErrVLMUnreachable)` | **PASS** | `TestING011_AC1_MalformedURLIsUnreachable` runs 7 subtests (`not a url`, `vlm.local`, `http://`, `ftp://example.com`, `vlm.local:2525`, `http://[::1`, `/var/run`); every one returns non-nil `err` with `errors.Is(err, tagging.ErrVLMUnreachable) == true`. This exercises both failure routes — `http.NewRequestWithContext` parse failure and the transport "unsupported scheme / no host" failure — so the wrap is proven regardless of where the malformed URL is rejected. |
| AC1 startup clause (spec: `07-architecture.md` line 121 "No URL-format validation at startup") — a malformed value is accepted at startup and surfaces later | **PASS** | `TestING011_AC1_MalformedURLAcceptedAtStartup` subtests `not a url`, `vlm.local`, `http://` — `config.Load()` returns nil error and `cfg.VLMURL` equals the raw value carried through. Confirms no new startup URL validation was added. Spec cross-check: `07-architecture.md` `VLM_URL` row; `06-decisions.md` "Env var bounds" (lines 9–13) ✓. |
| AC2 — Given `VLM_URL` is well-formed but the host is unreachable / When `Tag` is called / Then the error is still `ErrVLMUnreachable` (ING-002 unchanged) | **PASS** | `TestING011_AC2_WellFormedUnreachableStillUnreachable` subtests connection-refused (closed `httptest` server) and DNS failure (`http://ing011-does-not-resolve.invalid:2525`); both return `errors.Is(err, tagging.ErrVLMUnreachable) == true`. No regression to the ING-002 path. |
| AC3 — Given `VLM_URL` is empty or unset / When the server starts / Then it still exits with the startup error naming `VLM_URL` (ING-001 unchanged, no startup URL validation added) | **PASS** | Unit: `TestING011_AC3_EmptyOrUnsetURLStillFailsAtStartup` subtests `empty` and `unset`; both `config.Load()` errors naming `VLM_URL`. Process: `TestING001_Integration_AC1_MissingURLExits` (rerun) — real server exits code 1 with output containing `startup` and `VLM_URL`. |

**Spec-implied edge cases checked (no failures)**
- **Bad output is still not "unreachable"** — `TestING011_Edge_BadOutputIsStillNotUnreachable`:
  a reachable VLM returning HTTP 500 stays a status error and is **not**
  `ErrVLMUnreachable`, so the malformed-URL wrap did not swallow ING-002's
  connectivity-vs-output distinction.
- **Both malformed-URL failure points are wrapped** — a malformed URL can
  fail either at request construction (parse error) or at transport
  (unsupported scheme / missing host); the 7-case table deliberately covers
  both, confirming the ticket's fix (wrapping `NewRequestWithContext`) plus
  the pre-existing transport wrap leave no gap.
- **Trailing-slash trimming does not mask the failure** — `NewClient`
  trims the trailing `/`, so `"http://"` becomes `"http:"`; the resulting
  URL still fails and is correctly classified (subtest `scheme only`).

**Pre-existing failure (out of scope, not caused by this ticket)**
- `TestING001_Edge_WhitespaceURLIsAccepted` still fails with
  `config.Load() error = missing required environment variable: VLM_URL`.
  `git diff --stat` confirms ING-011 modifies only
  `internal/tagging/client.go`, `internal/tagging/client_test.go`, and this
  ticket — `internal/config/config.go` and `tests/ing_001_config_test.go`
  are untouched (whitespace trimming landed in commit `c356101`). This is
  the same pre-existing failure already flagged in ING-010 (and ING-005);
  the stale test asserts the old gap, and the current trim-then-empty
  behavior is consistent with AC3's "empty" clause. No new follow-up
  ticket needed; it is already tracked.

**Hard-constraint check (`00-overview.md`)**
- Fully local: test uses `httptest`/stdlib only; no cloud or hosted
  inference path introduced. ✓
- No fine-tuning: a client-side error-classification change only. ✓
- One photo per garment: unaffected (URL/error handling, not photo
  handling). ✓

## Review

**Decision: APPROVED** — left at `Review`, ready for Ali's final pass and
merge. Reviewer does not set Done.

Reviewed the diff (`internal/tagging/client.go`,
`internal/tagging/client_test.go`; `tests/ing_011_malformed_url_test.go`
new; `backlog/ING-011.md` modified — `git diff --stat` confirms no other
file touched) against `07-architecture.md` "Env var contract" (`VLM_URL`
row, line 121: "No URL-format validation at startup; a malformed value is
accepted and surfaces when a request is attempted as a 'VLM unreachable'
error (ING-002)"), `06-decisions.md` "Env var bounds" entry (lines 9–13),
`00-overview.md`'s hard constraints, and the ticket's three acceptance
criteria.

**Spec intent (not just AC wording)**
- The decision log requires exactly this shape: the malformed value is
  **accepted at startup** (no hand-rolled URL check) and surfaces on first
  use as the **ING-002 `ErrVLMUnreachable`**, because "that distinct,
  caller-handled error path already exists". The change wraps the
  `http.NewRequestWithContext` error with `%w` on `ErrVLMUnreachable`,
  leaving startup validation untouched, so a malformed URL now lands on the
  same sentinel the caller already handles. ✓
- Both failure routes are covered. A malformed base URL can fail either at
  request construction (parse error, newly wrapped) or at transport
  (unsupported scheme / missing host, wrapped by the pre-existing
  `c.http.Do` branch). The Tester's 7-case table exercises both, so there
  is no gap left between the new wrap and the existing one. ✓
- No regression to ING-002's distinction: a well-formed-but-unreachable
  host still returns `ErrVLMUnreachable` (unchanged transport path), and a
  reachable VLM returning bad output (HTTP 500) is still **not**
  `ErrVLMUnreachable` (`TestING011_Edge_BadOutputIsStillNotUnreachable`),
  so connectivity and bad-output classification remain separate. ✓
- No new startup validation: `config.Load()` still only trims + rejects an
  empty/unset `VLM_URL` (ING-001); malformed non-empty values are carried
  through unchanged. AC3's empty/unset path is unchanged. ✓
- Prerequisite spec update has landed: the `VLM_URL` row
  (`07-architecture.md:121`) and the "Env var bounds" entry
  (`06-decisions.md:9-13`) both state accept-then-surface-on-use. No
  protected file was touched by this ticket. ✓

**Hard constraints (`00-overview.md`) — no violations**
- **Fully local:** one line wrapping an existing local request-construction
  error; no cloud service, hosted inference, or network path added. ✓
- **No fine-tuning:** an error-classification change, not training. ✓
- **One photo per garment:** `Tag` still sends one image; URL handling only. ✓

**Standards / non-silent failure**
- No silent swallowing: the construction failure is still returned, now
  additionally classified via a `%w` chain (multiple-`%w`, the same pattern
  the transport branch already uses), so `errors.Is` works for both the
  sentinel and the underlying cause. No malformed input is coerced or
  dropped. ✓
- No taxonomy/schema surface involved (request construction, not VLM output
  parsing), so no `03-taxonomy.md`/`04-data-schema.md` enum validation
  applies. ✓
- Scope is minimal and on-ticket: `internal/tagging/client.go` (wrap site)
  plus its white-box test, and Tester's black-box
  `tests/ing_011_malformed_url_test.go`. In particular, `Tag`'s taxonomy
  validation and the ING-005 retry/flag path are untouched. ✓
- The noted pre-existing failure `TestING001_Edge_WhitespaceURLIsAccepted`
  (whitespace-only `VLM_URL` rejected since commit `c356101`) is genuinely
  out of scope: `internal/config/config.go` and `tests/ing_001_config_test.go`
  are not in this diff. Tracked separately; not a reason to hold this
  ticket. ✓

**Non-blocking note** (does not gate merge)
1. `ErrVLMUnreachable`'s doc comment (`client.go:24-28`) and `Tag`'s doc
   comment (`client.go:175-178`) still enumerate only "connection refused,
   timeout, DNS failure" as the causes. A malformed URL is now
   intentionally in this class too; folding "malformed base URL" into those
   comments on a future touch would keep the sentinel's documented contract
   complete. The behavior is correct and the wrap-site comment explains it,
   so no send-back.

**Merge note for Ali**
Ready for final review and merge. Touches only
`internal/tagging/client.go`, `internal/tagging/client_test.go`, the new
`tests/ing_011_malformed_url_test.go`, and this ticket. No protected spec
file, no `.env`/`.env.example`, no taxonomy/schema enum changed.

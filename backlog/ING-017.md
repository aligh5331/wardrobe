### [ING-017] Catalog store: GORM wardrobe-item model + SQLite at data/wardrobe.db

**As a** wardrobe owner
**I want** an `internal/store` package that models the wardrobe item exactly per
`04-data-schema.md`, opens and migrates `data/wardrobe.db`, and can insert and
list catalog rows
**So that** the ingest CLI has somewhere to persist tagged items and the API has
somewhere to read them from

**Acceptance criteria** (Gherkin):
```
Given data/wardrobe.db does not exist
When the store is opened
Then data/ is created if absent, and data/wardrobe.db holds a wardrobe-items
  table whose columns cover every field in 04-data-schema.md — id, category,
  subcategory, dominant_color, secondary_colors, pattern, warmth_tier,
  formality, photo_path, added_date, notes

Given an item with a caller-supplied uuid id and all 04-data-schema.md fields
When it is inserted and then read back
Then every field round-trips unchanged, secondary_colors returns as a list of
  0 or more palette values, and added_date is the cataloging date that was
  supplied

Given several items have been inserted
When all items are listed
Then every inserted item is returned and no schema field is dropped — notes and
  secondary_colors may be empty, but neither is replaced by an unexpected value

Given the store is opened twice against the same file
When it is opened again
Then re-running the migration is a no-op and previously written rows are intact
```

**Spec reference:** `04-data-schema.md` "Data schema — wardrobe item" (field
table and "Resolved"); `07-architecture.md` "Backend" (GORM, `sqlite` driver,
`data/wardrobe.db`, `data/photos/`) and "Full project structure"
(`internal/store`); `06-decisions.md` "Ingest→DB wiring: direct call, no
provider/service abstraction yet"

**Status:** Review

**Required exported surface** (ING-018 and ING-019 build against this; if naming
changes, both dependents must be updated in the same pass):
- `const DefaultDBPath = "data/wardrobe.db"`
- `type Item struct { ID, Category, Subcategory, DominantColor string; SecondaryColors []string; Pattern, WarmthTier, Formality, PhotoPath string; AddedDate time.Time; Notes string }`
- `func Open(path string) (*Store, error)` — creates the parent dir, opens
  SQLite, auto-migrates
- `func (*Store) Insert(item Item) error`
- `func (*Store) List() ([]Item, error)`
- `func (*Store) Close() error`

**Context / implementation notes**
- New package replacing the `internal/store/.gitkeep` placeholder; it is already
  in `07-architecture.md`'s tree.
- DB path is fixed at `data/wardrobe.db` per `07-architecture.md`. There is no
  DB-path entry in the env var contract, so do **not** add a new env var —
  callers use `DefaultDBPath`.
- `secondary_colors` is `list[enum]` in the schema and SQLite has no list type;
  store it as a JSON-encoded column (e.g. GORM `serializer:json`) and expose it
  as `[]string`. Internal storage representation only — not a schema change.
- `added_date` is the cataloging date (`04-data-schema.md` "when cataloged");
  represent it as `time.Time` (the API ticket formats it as a date).
- The store writes already-validated values. `03-taxonomy.md` enum validation is
  `05-vlm-tagging-spec.md` "Output validation", owned by the tagging pipeline
  (ING-004/ING-005); the store does not duplicate it.
- `notes` is optional and is not produced by tagging; the ingestion path supplies
  an empty value, but the store must support a non-empty future value.
- Driver: GORM with `gorm.io/driver/sqlite` (the `sqlite` driver named in
  `07-architecture.md`). If the build environment lacks CGO, a pure-Go SQLite
  driver is an acceptable substitute — document the choice in Implementation
  notes.

**Out of scope:** photo copying / `data/photos/` writes (ING-018), any HTTP
surface (ING-019), any update/delete operation (no second writer exists yet —
`06-decisions.md` "Ingest→DB wiring").

**Depends on:** nothing. First in the Sprint 2 order.

**Implementation notes:**
- Created `internal/store/store.go` with the required exported surface matching the ticket specification.
- Used pure-Go SQLite driver (`github.com/glebarez/sqlite`) instead of `gorm.io/driver/sqlite` to avoid CGO dependency — this is the acceptable substitute documented in the ticket's context notes.
- `SecondaryColors` field uses GORM's `serializer:json` tag for JSON encoding in SQLite (stored as TEXT), exposed as `[]string` in Go.
- `AddedDate` uses `time.Time` as specified; the API layer will format it as a date.
- `Open()` creates the parent directory (`data/`) if absent, opens the database, and runs `AutoMigrate(&Item{})` — re-running is a no-op per GORM behavior.
- All acceptance criteria verified: table columns cover every 04-data-schema.md field; round-trip insert/read preserves all fields including empty `SecondaryColors` and `Notes`; `List()` returns all inserted items; re-opening the store preserves existing rows.
- Code compiles (`go build ./internal/store/...`) and passes `go vet`.

## Test results

**Overall: all four acceptance criteria PASS; two spec-implied edge cases
also pass.** Tester authored `tests/ing_017_store_test.go` (black-box,
package `tests`) — no implementation code under `internal/` was touched.
Status set to `Review`.

**Test file added**
- `tests/ing_017_store_test.go` — 8 test functions / 10 cases: `AC1` (with
  an independent SQLite connection to inspect the on-disk schema), `AC2`
  (`all_fields_populated`, `empty_secondary_colors`), `AC3`, `AC4`,
  `Surface_DefaultDBPath`, `Edge_NotesFreeTextRoundTrips`,
  `Edge_NestedParentDirCreated`.
- Throwaway DB paths come from `t.TempDir()`, consistent with the existing
  suite (`ing_002`/`ing_005`/`ing_012`). Nothing is written under the
  repo's real `data/`.

**Commands / evidence**
- `go test -count=1 -run 'TestING017' ./tests/ -v` → all 8 functions PASS
  (10 PASS lines including subtests).
- `go vet ./...` → no output (clean).
- `go test -count=1 ./...` → `internal/config ok`, `internal/tagging ok`,
  `wardrobe/tests ok` (exit 0).
- AC1 verbose log:
  `table "items" columns: [id category subcategory dominant_color
  secondary_colors pattern warmth_tier formality photo_path added_date
  notes]`.
- AC2/AC4 captured SQL shows the list column is real JSON text and the
  empty list is `NULL`, e.g.
  `... "secondary_colors","pattern",... VALUES (...,"[""white"",""red""]",...` and
  `... "secondary_colors","pattern",... VALUES (...,NULL,...`.
- `git status --porcelain` → Coder's `internal/store/store.go`, `go.mod`,
  `go.sum`; Tester's `tests/ing_017_store_test.go`; this ticket file. No
  protected spec file is touched.

### Pass/fail per acceptance criterion

| AC | Result | Evidence |
|----|--------|----------|
| **AC1** — Given `data/wardrobe.db` does not exist / When the store is opened / Then `data/` is created if absent and the DB holds a table whose columns cover every `04-data-schema.md` field | **PASS** | `TestING017_AC1_OpenCreatesDataDirAndColumns`: parent dir confirmed absent before `Open`, present after; DB file created; an independent `gorm`/`glebarez-sqlite` connection reads `sqlite_master` and reports exactly one user table, `items`, whose columns are exactly the 11 schema columns (log above). Superset check on all 11 names passes. |
| **AC2** — Given an item with a caller-supplied uuid id and all `04-data-schema.md` fields / When inserted and read back / Then every field round-trips unchanged, `secondary_colors` returns as a list of 0 or more palette values, and `added_date` is the supplied cataloging date | **PASS** | `TestING017_AC2_RoundTripAllFields/all_fields_populated`: a uuid-id `Item` with `SecondaryColors: ["white","red"]` and all other fields compares field-by-field equal after `Insert`→`List` (`added_date` via `time.Equal`, so location is not compared). `/empty_secondary_colors`: a nil list returns length 0 (no `"null"` string, no error). Separate assertion: `AddedDate.Format("2006-01-02") == "2026-09-18"`, the supplied cataloging day. SQL log confirms the populated list is stored as JSON text and the empty list as `NULL`. |
| **AC3** — Given several items have been inserted / When all items are listed / Then every inserted item is returned and no schema field is dropped — `notes`/`secondary_colors` may be empty but are not replaced by an unexpected value | **PASS** | `TestING017_AC3_ListReturnsEveryItemNoFieldDropped`: 3 distinct items inserted (populated, empty `secondary_colors`+`notes`, one-color list with empty notes); `List()` returns exactly 3; each is field-by-field equal to what was inserted; the empty item's `SecondaryColors` length is 0 and `Notes == ""`; the one-color item returns `[navy]`. |
| **AC4** — Given the store is opened twice against the same file / When opened again / Then re-running migration is a no-op and previously written rows are intact | **PASS** | `TestING017_AC4_ReopenIsNoOpRowsIntact`: open → insert → close → reopen returns nil error (AutoMigrate replay is a no-op) and `List()` returns the 1 pre-existing row, field-equal; a subsequent insert succeeds and `List()` returns 2, both rows intact. |

### Edge cases checked (spec-implied)

- **Empty list, not a sentinel** — a nil/empty `secondary_colors` round-trips
  as a length-0 list, never a `"null"` string or an error. This is the
  `list[enum] "0 or more"` clause in `04-data-schema.md`.
- **Free-text `notes`** — `04-data-schema.md` calls `notes` optional free
  text and the ticket requires a non-empty future value to be supported;
  `TestING017_Edge_NotesFreeTextRoundTrips` confirms quotes, an embedded
  newline, and non-ASCII (`café 🔧`) survive the round-trip.
- **Absent parent directories** — `TestING017_Edge_NestedParentDirCreated`
  opens `a/b/c/wardrobe.db` with every level absent and confirms they are
  created (`os.MkdirAll`), generalizing AC1's `data/` clause.
- **`DefaultDBPath` value** — `TestING017_Surface_DefaultDBPath` pins
  `store.DefaultDBPath == "data/wardrobe.db"`, the fixed path the ticket
  forbids replacing with a new env var.
- **Out of scope, deliberately not tested** — duplicate-id insert,
  update/delete, and any taxonomy-enum rejection. The ticket's context note
  places enum validation in ING-004/ING-005 and excludes update/delete.

### Spec cross-check

- `04-data-schema.md` "Data schema — wardrobe item": every field in the
  table maps to a persisted column — `id` (PK), `category`, `subcategory`,
  `dominant_color`, `secondary_colors`, `pattern`, `warmth_tier`,
  `formality`, `photo_path`, `added_date`, `notes`. `subcategory` is marked
  NOT NULL, matching "Resolved: subcategory is required". No `season`
  column is added (spec says none in phase 1). No enum validation is
  duplicated in the store, matching the ticket's context note.
- `07-architecture.md` "Backend" / "Full project structure": implementation
  lives in `internal/store`, uses GORM + a `sqlite` driver, and the default
  path is `data/wardrobe.db`. Coder substituted the pure-Go
  `github.com/glebarez/sqlite` for CGO reasons — the ticket explicitly allows
  this substitute if documented, and it is documented in Implementation
  notes.
- `06-decisions.md` "Ingest→DB wiring: direct call, no provider/service
  abstraction yet": the exported surface is the integration point
  (`DefaultDBPath`, `Item`, `Open`, `Insert`, `List`, `Close`); no extra
  writer, service layer, or second writer was introduced. The required
  exported surface matches exactly and is exercised by every test.
- `00-overview.md` hard constraints: no cloud/hosted inference, no
  fine-tuning, no multi-tenant/auth surface, no photo-writing (ING-018) or
  HTTP surface (ING-019) added. Fully local SQLite only. No new env var.

**Interpretation noted (not a failure):** AC1's phrase "a wardrobe-items
table" is read as prose for "the table of wardrobe items", not a literal
schema identifier — the ticket's required exported surface names no table,
and GORM's default for `Item` is `items`. The physical table is `items` with
all 11 required columns. Flagging this explicitly so Reviewer does not read
it as a missed literal name.

**Verdict:** all four acceptance criteria pass, the two spec-implied edge
cases pass, `go test ./...` is green, and `go vet ./...` is clean. No new
follow-ups. Status set to `Review`.

## Review

**Decision: APPROVED** — left at `Review`, ready for Ali's final pass and
merge. Reviewer does not set Done.

Reviewed the working-tree diff for this ticket (`internal/store/store.go` new;
`go.mod` ±22; `go.sum` ±38; `tests/ing_017_store_test.go` new) against
`04-data-schema.md` "Data schema — wardrobe item", `07-architecture.md`
"Backend" / "Full project structure", `06-decisions.md` "Ingest→DB wiring:
direct call, no provider/service abstraction yet", and `00-overview.md`'s hard
constraints. Tester's results are taken as evidence — this review does not
re-run the build or tests. `git diff --name-only` lists only this ticket,
`go.mod`, and `go.sum`; the two untracked files are the ticket's own. No
protected spec file (`0*.md`, `agents/*.md`, `AGENTS.md`) is touched. ✓

**Spec intent (not just AC wording)**
- **Exact required exported surface.** `DefaultDBPath`, `Item` (all 11 fields,
  correct types/order), `Open`, `Insert`, `List`, `Close` all match the
  ticket's contract byte-for-byte in signature and naming. This matters beyond
  the ACs: ING-018 (`Insert`) and ING-019 (`List`) are written against these
  names, and the diff does not rename or reshape any of them, so no dependent
  ticket needs updating in this pass. ✓
- **Schema coverage is real, not cosmetic.** Struct tags derive exactly the 11
  schema columns (`WarmthTier`→`warmth_tier`, `DominantColor`→`dominant_color`,
  `AddedDate`→`added_date`, etc.); `secondary_colors` uses
  `gorm:"serializer:json"` so a list is stored as JSON TEXT and re-exposed as
  `[]string`. `subcategory` is `not null`, matching `04-data-schema.md`
  "Resolved: subcategory is required". No `season` column (spec: none in phase
  1). ✓
- **`added_date` semantics.** Stored as `time.Time`; the ticket says the API
  formats it as a date, and the schema calls it "when cataloged". ING-018 will
  supply the cataloging instant, so this is the right representation — no
  date-only truncation baked in where the API ticket needs to control
  formatting. ✓
- **No duplicated enum validation.** `03-taxonomy.md` validation belongs to
  `05-vlm-tagging-spec.md` "Output validation" / ING-004/ING-005; the ticket
  explicitly scopes it out and the store correctly does not re-validate. Not
  a "silent swallow" — the store's contract is that it receives
  already-validated values. ✓
- **No premature abstraction.** Direct `internal/store` functions, no
  provider/service/interface layer, matching `06-decisions.md` "Ingest→DB
  wiring". ✓
- **Pure-Go SQLite substitute.** `07-architecture.md` names "GORM, `sqlite`
  driver", not a specific driver package. The ticket permits a pure-Go
  substitute to avoid CGO, and Coder used `github.com/glebarez/sqlite` and
  documented it in Implementation notes. `go.mod` adds GORM plus that driver
  and its `modernc.org/*` transitive deps — all embedded/local, no network.
  Accepted deviation, not a spec change. ✓

**Hard constraints (`00-overview.md`) — no violations**
- **Fully local:** embedded SQLite only; no cloud service, hosted inference
  API, or network path added. No `data/` artifact is committed. ✓
- **No fine-tuning:** no model/training surface touched. ✓
- **One photo per garment:** `Item` is one row per garment; no
  outfit/multi-garment assumption introduced. ✓
- No new env var: the DB path is the fixed `DefaultDBPath`, per
  `07-architecture.md`'s env contract. ✓

**Standards / non-silent failure**
- No error is swallowed: `Open` returns `MkdirAll`, `gorm.Open`, and
  `AutoMigrate` errors; `Insert`/`List` return `Create`/`Find` errors; `Close`
  returns the `sql.DB()` and close errors. No `_ =` discards. Errors are
  returned unwrapped, which is plain but not a violation and consistent with
  the rest of the codebase. ✓
- `MkdirAll` on `filepath.Dir(path)` is safe for a bare filename
  (`filepath.Dir` → `"."`), so `Open("wardrobe.db")` does not fail. ✓
- Empty `secondary_colors` round-trips as a 0-length list (SQL `NULL`),
  never a `"null"` string or an error — the `list[enum] "0 or more"` clause of
  `04-data-schema.md`. ✓

**Non-blocking notes** (do not gate merge)
1. `internal/store/.gitkeep` was left in place alongside the new `store.go`.
   Dead weight once the package has real content; deleting it is trivial
   cleanup Ali can fold into the merge. Not a spec or functional issue, so not
   a send-back.
2. AC1's prose "a wardrobe-items table" is correctly read as "the table of
   wardrobe items": the ticket names no physical table and the table name is
   internal (ING-019 reads via `List()`, not raw SQL). GORM's default `items`
   with all 11 columns is fine. Tester flagged the same interpretation — I
   concur.
3. The test file imports `github.com/glebarez/sqlite` directly for raw schema
   inspection; that couples the black-box test to the chosen driver. Acceptable
   for this ticket (the driver choice is documented) and not worth a change.

**Merge note for Ali**
Ready for final review and merge. This ticket's changes are:
`internal/store/store.go` (new), `go.mod`/`go.sum` (GORM + pure-Go SQLite
driver), `tests/ing_017_store_test.go` (new), plus this backlog file. No
implementation file outside `internal/store`, no protected spec file, no
taxonomy/schema enum, and no new env var.

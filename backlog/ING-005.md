### [ING-005] Retry once on malformed/invalid VLM output, then flag

**As a** wardrobe owner
**I want** a malformed or taxonomy-invalid VLM response retried exactly
once at a nonzero temperature, and flagged for manual review only if
the retry also fails
**So that** a one-off model glitch doesn't cost a manual review, while
a genuinely bad tag still gets caught with full diagnostic detail

**Policy** (settled — see `06-decisions.md`):
- Attempt 1: send the tagging request (ING-002), validate the response
  (ING-004).
- If attempt 1 fails validation (malformed JSON, invalid enum, missing
  required field): log attempt 1's full detail, then send attempt 2 —
  same photo, same prompt, `VLM_TEMPERATURE`-sampled, so it's an
  independent draw, not a deterministic repeat.
- If attempt 2 also fails validation: log attempt 2's full detail, and
  flag the item for manual review with **both** attempts' logs
  attached.
- If either attempt passes validation, use that result and stop — no
  need to burn a second call on a success.

**Acceptance criteria** (Gherkin):
```
Given a VLM response on attempt 1 that fails validation (malformed
  JSON, invalid enum, or a missing required field)
When it is processed
Then attempt 1's full detail is logged (see log format below), and a
  second request is sent for the same photo before anything is flagged

Given attempt 2's response passes validation
When it is processed
Then the validated result is used normally — the item is NOT flagged,
  even though attempt 1 failed

Given attempt 2's response also fails validation
When it is processed
Then the item is flagged for manual review, with both attempt 1's and
  attempt 2's logged detail attached — not just the final failure

Given a flagged item
When the ingestion pipeline continues
Then processing of other photos in the same batch is not blocked by
  one bad result

Given the VLM is unreachable (ING-002's distinct error, not a
  validation failure)
When it occurs on either attempt
Then it is NOT treated as a malformed-output case and does NOT count
  toward the two-attempt policy — connectivity failure is a different
  problem and should surface immediately, not be silently retried into
  a flag
```

**Log format** (needed for both diagnosis now and calibration later —
see `later-ideas.md`'s VLM calibration idea, which this incidentally
feeds): append one JSON object per attempt to `logs/vlm-attempts.jsonl`
(under the existing gitignored `logs/` directory, `07-architecture.md`).
Fields:
```json
{
  "item_id": "<shared id linking attempt 1 and attempt 2 for the same photo>",
  "timestamp": "2026-09-13T14:32:01Z",
  "photo_path": "data/photos/xxxxx.jpg",
  "attempt": 1,
  "temperature": 0.4,
  "raw_response": "<full raw text returned by the VLM, unmodified>",
  "parsed_json": null,
  "failure_type": "malformed_json",
  "failure_detail": "response was not valid JSON: unexpected token at position 142"
}
```
For a validation failure (JSON parsed fine, but content is invalid),
`parsed_json` holds the parsed object and `failure_type` is
`"invalid_enum"` or `"missing_required_field"` instead, with
`failure_detail` naming the specific field/value.

`item_id` must be shared across both attempts for the same photo so a
later analysis can join them — that pairing (did attempt 2 agree with
attempt 1, or land somewhere completely different?) is the actual
signal `later-ideas.md`'s calibration idea is after, now collected as
a side effect of normal use instead of needing its own harness.

**Spec reference:** `05-vlm-tagging-spec.md` "Output validation";
`06-decisions.md` "Malformed VLM output: retry once at nonzero
temperature, then flag"; `later-ideas.md` "VLM tagging calibration via
scraped catalog images"

**Status:** Review

## Implementation notes

Implemented the retry-once/flag policy as a processor in the existing
`internal/tagging` package, plus the per-attempt JSONL log the ticket
specifies.

**Files**
- `internal/tagging/processor.go` (new) — `Outcome`,
  `Processor`, `NewProcessor`, `WithAttemptLogger`, and
  `(*Processor).Process(ctx, photoPath)`. Also the `newItemID` UUIDv4
  generator and the per-attempt record builder.
- `internal/tagging/attempt_log.go` (new) — `AttemptLog` (the ticket's
  9 fields), the `AttemptLogger` seam, `JSONLAttemptLogger` (appends one
  JSON object per line, creates `logs/` if absent), and
  `DefaultAttemptLogPath = "logs/vlm-attempts.jsonl"`.
- `internal/tagging/validate.go` — added the `FailureType*` constants and
  a `ValidationError` type carrying `FailureType` + `FailureDetail`.
  `ParseTaggingResult` and its helpers now return it. Error text and
  `errors.Is(err, ErrInvalidTaggingResult)` are unchanged, so ING-004
  behavior/tests are preserved.
- `internal/tagging/client.go` — added `(*Client).Temperature()` so the
  attempt log can record the temperature used.
- `internal/tagging/processor_test.go` (new) — unit self-checks.
- `internal/tagging/validate_test.go` — added
  `TestParseTaggingResult_FailureTypes`.

**Behavior**
- `Process` sends attempt 1 via `Client.Tag`, validates with
  `ParseTaggingResult`, and logs the attempt (full detail) before doing
  anything else. On validation failure it sends attempt 2 for the same
  photo (the client reads `VLM_TEMPERATURE` afresh per call per ING-007).
  If attempt 2 passes, its result is used and the item is **not**
  flagged; if it also fails, `Outcome.Flagged` is true and both attempts
  are attached in `Outcome.Attempts`.
- A flag returns a **nil** error, so a batch caller keeps going; a hard
  error (VLM unreachable, non-2xx/envelope, or a failed log write)
  returns non-nil. `errors.Is(err, ErrVLMUnreachable)` distinguishes the
  connectivity case, which is never retried and never flagged (AC5).
- `failure_type` is one of `malformed_json`, `invalid_enum`,
  `missing_required_field`; `failure_detail` names the field/value (or
  wraps the JSON error). `parsed_json` holds the parsed object when the
  raw text was valid JSON, and `null` otherwise.

**Assumptions / deviations (for Tester + Reviewer)**
1. **Successes are logged too.** The ticket says "append one JSON object
   per attempt"; I read that literally and log every attempt, with
   `failure_type`/`failure_detail` empty on success. This is what makes
   the calibration pairing in `later-ideas.md` possible for the common
   attempt-1-fails/attempt-2-succeeds case (you need attempt 2's parsed
   answer to compare). Easy to restrict to failures only if that reading
   is preferred.
2. **`item_id` is generated per `Process` call** (UUIDv4, stdlib
   `crypto/rand`), because no caller supplies one yet and it must be
   shared across both attempts. `04-data-schema.md` types the item id as
   a uuid, so the eventual store row can reuse it and the log will join
   to it. ING-012 does not need to generate one.
3. **Only validation failures are retried.** Any error from
   `Client.Tag` (unreachable, non-2xx status, unusable envelope) surfaces
   immediately without a second attempt or a flag. The policy text names
   connectivity specifically; I extended that to all `Tag` errors since
   none of them is a raw model response to validate. Flag if a retry for
   transient server errors is wanted.
4. **"Flag" is `Outcome.Flagged` + attached attempts**, not a DB row —
   no store exists yet (ING-012 is out of scope). ING-012 reports the
   flagged photo separately.
5. **Batch continuation is the caller's loop.** `Process` guarantees a
   flag is not an error; the batch loop itself belongs to ING-012. AC4
   is demonstrated by `TestProcessor_Process_FlagDoesNotBlockNextPhoto`.
6. **Temperature is read twice** (once by `Process` for the log, once by
   `Tag` for the payload). Production config is static, so they match;
   the log could differ only if the source changed between the two reads.
7. `validate.go` now returns a structured error type — a small change to
   already-`Done` ING-004 code, needed to populate `failure_type` without
   string-matching error messages.

**Validation performed**
- `go build ./...` → clean.
- `gofmt -l internal/ tests/` → clean; `go vet ./...` and
  `go vet -tags=integration ./...` → clean.
- `go test -race -count=1 -timeout 180s ./internal/tagging/` → **ok**
  (new processor/logger tests and all ING-002/003/004/007 tests pass).
- `go test -race -count=1 -timeout 180s ./...` → `internal/config` ok,
  `internal/tagging` ok. The `tests` package has **one pre-existing
  failure unrelated to this ticket**:
  `TestING001_Edge_WhitespaceURLIsAccepted` expects a whitespace-only
  `VLM_URL` to be accepted, but commit `c356101` ("reject empty string
  for url env") made `Load` trim + reject it; the test predates that
  commit (`tests/ing_001_config_test.go` last changed in `5fc4bba`,
  config.go's reject behavior added in `c356101`). Not touched here —
  flagged for Planner/Ali as a stale test (ING-011 is the nearby
  malformed-URL ticket).
- Spec cross-check: the log object has exactly the ticket's 9 fields
  (`item_id`, `timestamp` RFC3339 UTC, `photo_path`, `attempt`,
  `temperature`, `raw_response`, `parsed_json`, `failure_type`,
  `failure_detail`); failure types map one-to-one to
  `05-vlm-tagging-spec.md` "Output validation"; the retry/flag policy
  matches `06-decisions.md` "Malformed VLM output…"; log path is under
  the gitignored `logs/` per `07-architecture.md`.
- **Live E2E** against the real VLM at `http://192.168.0.100:2525`
  with a scratch probe in `temp/` (removed after): the processor sent a
  synthetic garment image, got valid taxonomy JSON on attempt 1
  (`top`/`t-shirt`/`navy`/`[]`/`solid`/`light`/`casual`, not flagged),
  and appended exactly one `logs/vlm-attempts.jsonl` line matching the
  ticket's format — shared `item_id`, RFC3339 UTC `timestamp`,
  `photo_path`, `attempt: 1`, `temperature: 0.4`, unmodified
  `raw_response`, populated `parsed_json`, empty failure fields. The
  retry/flag paths are covered by the stub-backed unit tests above; the
  live model happened to succeed on the first attempt.

## Test results

Acceptance suite: `tests/ing_005_retry_test.go` (black-box, package
`tests`, no build tag). No implementation code was modified. Valid and
invalid payloads are derived from `03-taxonomy.md` at runtime
(`loadTaxonomy`/`taggingJSON`), so the suite checks the spec vocabulary
rather than a hand-copied enum list.

**Commands / evidence**
- `go test -race -count=1 -run TestING005 ./tests/ -v` → all PASS
  (AC1–AC5, log format, 3 edge tests).
- `go test -race -count=3 -timeout 300s -run TestING005 ./tests/` → ok
  across 3 runs (no flakes).
- `go test -race -count=1 -timeout 300s ./...` → `internal/config` ok,
  `internal/tagging` ok, `tests` has **one pre-existing failure
  unrelated to this ticket** (see below).
- `gofmt -l tests/` and `go vet ./...` → clean.

**Pass/fail per acceptance criterion**

| AC | Result | Evidence |
|---|---|---|
| AC1 — attempt 1 fails validation (malformed JSON, invalid enum, or missing required field) → attempt 1's full detail logged, and a second request sent for the same photo before anything is flagged | **PASS** | `TestING005_AC1_Attempt1FailureRetriedAndLogged` runs all three failure modes as subtests: exactly 2 requests, attempt 2 carries the **identical image data URI and prompt** (same photo), `Flagged == false`, 2 in-memory attempt records, and attempt 1's record passes the full-field check (shared non-empty `item_id`, RFC3339 UTC `timestamp`, `photo_path`, `attempt: 1`, `temperature: 0.4`, unmodified `raw_response`, correct `failure_type`, `failure_detail` naming the specific value/field, `parsed_json` null for malformed and the parsed object otherwise). `TestING005_Edge_Attempt1LoggedBeforeRetry` additionally proves the ordering: the stub had received exactly 1 request when attempt 1 was logged and 2 when attempt 2 was logged, so the first failure is persisted before the retry is sent. |
| AC2 — attempt 2 passes validation → validated result used, item NOT flagged even though attempt 1 failed | **PASS** | `TestING005_AC2_RetrySuccessUsesResultNotFlagged`: attempt 1 malformed, attempt 2 valid → `Flagged == false`, `Result` deep-equals the spec-derived `TaggingResult`, exactly 2 requests, attempt 1 `malformed_json` and attempt 2 clean (empty failure fields). |
| AC3 — attempt 2 also fails → item flagged with **both** attempts' logged detail attached, not just the final failure | **PASS** | `TestING005_AC3_SecondFailureFlagsWithBothAttempts`: attempt 1 malformed, attempt 2 invalid enum → `Flagged == true`, zero `Result`, exactly 2 requests (no third attempt), `Outcome.Attempts` has both, logger has both; attempt 1 retains `malformed_json` and attempt 2 retains `invalid_enum` with distinct, non-empty details, all sharing one `item_id`. |
| AC4 — a flagged item does not block other photos in the same batch | **PASS** | `TestING005_AC4_FlaggedPhotoDoesNotBlockBatch`: a 3-photo batch where photo 1 fails both attempts → the loop continues; exactly 1 flagged, 2 tagged with validated results, 4 requests and 4 log lines total. `Process` returns a nil error for a flag, so the caller's loop is never aborted. |
| AC5 — VLM unreachable on either attempt is NOT treated as malformed output and does NOT count toward the two-attempt policy; surfaces immediately | **PASS** | `TestING005_AC5_UnreachableNotRetriedOrFlagged` has two subtests. attempt 1: connection dropped → exactly **1** request (no retry), `errors.Is(err, ErrVLMUnreachable)`, `Flagged == false`, 0 log records. attempt 2: attempt 1 malformed (logged), attempt 2 connection dropped → error is `ErrVLMUnreachable`, `Flagged == false`, only attempt 1's `malformed_json` record exists (the connectivity failure was not converted into a flag). |

**Log format checked against the ticket (`TestING005_LogFormat_JSONL`)**
- `DefaultAttemptLogPath == "logs/vlm-attempts.jsonl"`, and
  `NewJSONLAttemptLogger` creates the parent `logs/` directory when
  absent (file written into a fresh `t.TempDir()`).
- Exactly one JSON object per line, 2 lines for a failed-then-passed
  photo; each line has **exactly the ticket's 9 keys** — `item_id`,
  `timestamp`, `photo_path`, `attempt`, `temperature`, `raw_response`,
  `parsed_json`, `failure_type`, `failure_detail`.
- `item_id` shared across attempt 1 and attempt 2 (and equal to
  `Outcome.ItemID`); `timestamp` parses as RFC3339 and is UTC (trailing
  `Z`); `raw_response` byte-for-byte unmodified; `attempt` 1 then 2;
  `temperature` 0.4; line 1 `parsed_json` is literal `null` with
  `failure_type: "malformed_json"` and a specific `failure_detail`; line
  2 `parsed_json` is the valid parsed object with empty failure fields.

**Spec-implied edge cases checked (no failures)**
- **Exact two-attempt cap:** a retry that also fails produces exactly 2
  requests, never a third (AC3).
- **Success does not retry:** a validating attempt 1 makes exactly 1
  request and 1 log record with empty failure fields
  (`TestING005_Edge_SuccessOnAttempt1Stops`) — the policy's "no need to
  burn a second call on a success".
- **Non-silent failure:** a log-write failure surfaces as a non-nil
  error, is not `ErrVLMUnreachable`, and is not turned into a flag
  (`TestING005_Edge_LogWriteFailureSurfaces`), so the diagnostic record
  cannot be lost silently.
- **Failure classification map:** `failure_type` values observed are
  exactly the three in `05-vlm-tagging-spec.md` "Output validation" and
  the ticket (`malformed_json`, `invalid_enum`,
  `missing_required_field`).
- **`bare ErrVLMUnreachable` vs validation:** the suite reuses ING-002's
  connectivity sentinel, so the retry policy never swallows a
  connectivity failure (AC5).

**Observations / follow-ups for Planner (non-blocking, no AC failure)**
1. **Successful attempts are also logged**, with `failure_type`/
   `failure_detail` as empty strings (Coder assumption #1). The ticket's
   format example describes a failure and does not state what a success
   line contains; logging it is what makes the attempt 1/attempt 2
   calibration pairing in `later-ideas.md` usable, and the 9 fields are
   still present. Flag for Reviewer to confirm this reading; it is not
   an AC failure.
2. **`Outcome.Attempts` for a hard error is empty** even when attempt 1
   was already logged (AC5 attempt-2 case): `Process` returns
   `Outcome{}` with the error. The diagnostic is still on disk in the
   attempt log, but a caller that only reads the returned `Outcome`
   loses the first attempt. Worth a conscious decision when ING-012's
   batch/report layer consumes this API.
3. **Default log path is relative to the process CWD.** `logs/` at the
   project root is correct for production (`07-architecture.md`), but a
   caller running from another directory would write elsewhere unless a
   logger with an explicit path is supplied. The constant is correct;
   the wiring is the future handler's concern.
4. **Temperature is read twice per attempt** (once by `Process` for the
   log, once by `Tag` for the payload) — Coder assumption #6. The suite
   uses a stable source, so log and payload agree; a source that changed
   between the two reads could diverge. ING-007 already covers
   fresh-per-call reads; no action needed now.
5. **Pre-existing, unrelated failure in the same package:**
   `TestING001_Edge_WhitespaceURLIsAccepted` fails in isolation
   (`go test -run TestING001_Edge_WhitespaceURLIsAccepted ./tests/ -v`)
   because commit `c356101` made `config.Load` trim + reject a
   whitespace-only `VLM_URL`, while the older test still expects it to be
   accepted. It predates and is untouched by this ticket; flagged for
   Planner/Ali as a stale ING-001 test (ING-011 is the nearby
   malformed-URL ticket). This is the only failure in
   `go test ./...`.

**Flake note:** none observed. `-race` clean across `-count=3`; the AC
suite is deterministic, network-free (httptest stubs), and creates no
files outside `t.TempDir()`.

**Verdict: all five acceptance criteria PASS; no failures attributable
to ING-005. Status set to `Review`.**

## Review

**Decision: APPROVED** — left at `Review`, ready for Ali's final pass and
merge. Reviewer does not set Done.

Reviewed the diff (`internal/tagging/processor.go`, `attempt_log.go`,
`processor_test.go` new; `client.go`, `validate.go`, `validate_test.go`,
`backlog/ING-005.md` modified; `tests/ing_005_retry_test.go` new;
`git status` confirms nothing else changed) against
`05-vlm-tagging-spec.md` "Output validation", `06-decisions.md` "Malformed
VLM output: retry once at nonzero temperature, then flag",
`07-architecture.md` (the gitignored `logs/` directory and the
`VLM_TEMPERATURE` row), `later-ideas.md` "VLM tagging calibration via
scraped catalog images", and `00-overview.md`'s hard constraints.

**Spec intent (not just AC wording)**
- The decision log requires three things beyond the AC table: (a) retry
  **exactly once**, (b) the retry is a **genuine second sample**, and
  (c) **every attempt logged in full** keyed by a shared `item_id`.
  `Process`'s `maxAttempts = 2` loop delivers (a) with no third call;
  `Tag` reads `temperature()` afresh per request (ING-007) so attempt 2
  is an independent draw rather than a cached repeat (b); and
  `newAttemptLog` + `JSONLAttemptLogger` record every attempt — success
  and failure — under one generated `item_id` (c). The
  success-logging choice (Coder assumption #1) is not a deviation: the
  decision log says "Every attempt is logged in full", and the
  attempt-1-fails/attempt-2-succeeds pairing is precisely the
  calibration signal it is meant to feed. ✓
- `failure_type` maps one-to-one to the three rejection reasons in
  `05-vlm-tagging-spec.md` "Output validation" via the `FailureType*`
  constants; `FailureDetail` names the specific field/value rather than
  being a generic string. The `ValidationError` type carries the
  classification structurally, so nothing string-matches error text. ✓
- The log object has exactly the ticket's 9 fields, `timestamp` is
  RFC3339 UTC, `raw_response` is unmodified, and `parsed_json` is `null`
  for malformed JSON and the object otherwise. `DefaultAttemptLogPath`
  is under the gitignored `logs/` per `07-architecture.md`, and the
  logger creates the directory. ✓
- AC5's distinction is preserved at the type level: `Tag`'s transport
  failure wraps `ErrVLMUnreachable`, `Process` returns it immediately
  without a retry or a flag, and no log record is fabricated for a
  failure that never produced model text. Attempt-1-validation-failure →
  attempt-2-unreachable surfaces the connectivity error rather than
  converting it into a second malformed failure. ✓
- Touching already-`Done` `validate.go`/`client.go` is in service of the
  ticket (structured failure type, temperature accessor). The error text
  and `errors.Is(err, ErrInvalidTaggingResult)` are byte-for-byte
  unchanged through `ValidationError.Unwrap`, and ING-002/004 tests
  still pass, so no prior ticket's contract regressed. ✓

**Hard constraints (`00-overview.md`) — no violations**
- **Fully local:** stdlib only (`crypto/rand`, `encoding/json`, `os`,
  `net/http` against the configured local VLM). No cloud service or
  hosted inference introduced. The live E2E was against a local
  `192.168.x.x` server. ✓
- **No fine-tuning:** retry/validation logic and a JSONL log; nothing
  trains a model. ✓
- **One photo per garment:** `Process` takes one `photoPath`; `Tag`
  sends one image per request. No outfit/multi-garment assumption. ✓

**Standards / non-silent failure**
- No silent swallowing: a log-write failure surfaces as a non-nil error
  distinct from `ErrVLMUnreachable` and is not turned into a flag
  (`TestING005_Edge_LogWriteFailureSurfaces`); a malformed enum/missing
  field is rejected, never coerced or dropped. ✓
- Taxonomy validation still routes through `03-taxonomy.md`'s enums
  (`validCategories`/`validSubcategories`/`colorPalette`/…), and the
  black-box suite derives payloads from the taxonomy file at runtime, so
  it checks the spec vocabulary rather than a hand-copied list. ✓
- Tests exercise the exported API only and create no files outside
  `t.TempDir()`; `-race` clean across `-count=3` per Tester. ✓

**Non-blocking notes** (do not gate merge)
1. `newAttemptLog` decides `parsed_json` presence with `json.Valid(raw)`,
   while `failure_type` comes from the validation error. For valid JSON
   that is not an object (e.g. `"hello"` or `[]`), `ParseTaggingResult`
   correctly classifies `malformed_json`, but `json.Valid` is true so
   `parsed_json` would emit the scalar/array instead of `null`. The
   ticket ties `malformed_json` to `parsed_json: null`. Narrow edge,
   outside the ACs, and no AC fails; a future touch-up could drive
   `parsed_json` from whether validation produced a usable object. Not
   worth a send-back.
2. On a hard error after attempt 1 was logged (AC5 attempt-2 case),
   `Process` returns `Outcome{}`, so `ItemID`/first-attempt detail are
   not in the returned value even though attempt 1 is on disk (Tester
   observation #2). The AC only requires immediate surfacing; ING-012
   should decide whether its batch/report layer wants the first attempt
   attached. Non-blocking.
3. `Temperature()` and `Tag` each read the temperature source
   separately (Coder assumption #6), so a source that changed between
   the two reads could log a different value than the payload carried.
   Production config is static; ING-007's fresh-per-call contract is
   honored. Non-blocking.
4. Pre-existing, unrelated failure `TestING001_Edge_WhitespaceURLIsAccepted`
   (commit `c356101` made `config.Load` trim + reject a whitespace-only
   `VLM_URL`; the older test still expects acceptance). Predates and is
   untouched by ING-005 — for Ali/Planner triage alongside ING-011, not a
   reason to hold this ticket.

**Merge note for Ali**
Ready for final review and commit. Touches only `internal/tagging/`
(new `processor.go`/`attempt_log.go`, additions to `client.go`/
`validate.go`, tests), the new `tests/ing_005_retry_test.go`, and this
ticket. No protected spec file modified, no `.env`/`.env.example`
change, no taxonomy/schema enum invented.
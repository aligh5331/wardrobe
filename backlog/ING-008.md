### [ING-008] Guard floatEnv() against NaN, Inf, and out-of-range temperature values

**As a** wardrobe owner
**I want** `floatEnv()` to reject `"NaN"`, `"Inf"`, and temperature
values outside the acceptable range
**So that** a non-finite or out-of-range temperature cannot propagate
to the VLM client and break llama.cpp (flagged as non-blocking
follow-up in ING-006)

**Acceptance criteria** (Gherkin):
```
Given VLM_TEMPERATURE is set to "NaN"
When the server starts
Then it exits with a startup error naming VLM_TEMPERATURE as invalid

Given VLM_TEMPERATURE is set to "Inf" or "-Inf"
When the server starts
Then it exits with a startup error naming VLM_TEMPERATURE as invalid

Given VLM_TEMPERATURE is set to a negative value (e.g. "-1")
When the server starts
Then it exits with a startup error naming VLM_TEMPERATURE as invalid

Given VLM_TEMPERATURE is set to a value exceeding 1.0 (e.g. "1.5" or
  "100")
When the server starts
Then it exits with a startup error naming VLM_TEMPERATURE as invalid

Given VLM_TEMPERATURE is set to "0"
When the server starts
Then startup proceeds normally — 0 is a valid value (deliberate
  deterministic runs are allowed), it is simply not the default

Given VLM_TEMPERATURE is set to a valid finite value within 0.0–1.0
  inclusive (e.g. "0", "0.4", "0.7", "1.0")
When the server starts
Then startup proceeds normally and the value is available to the VLM
  client
```

**Spec reference:** `07-architecture.md` "Env var contract"
(`VLM_TEMPERATURE` row); `06-decisions.md` "VLM_TEMPERATURE acceptable
range: 0.0–1.0, default 0.4" and "Malformed VLM output: retry once at
nonzero temperature, then flag"; flagged as non-blocking follow-up in
`backlog/ING-006.md`

**Status:** Done

**Follow-up from:** ING-006 Reviewer decision, known issue #1

## Root cause

`floatEnv()` in `internal/config/config.go` uses `strconv.ParseFloat`,
which accepts `"NaN"`, `"Inf"`, and `"-Inf"` as valid `float64` values,
plus any finite float including negative values and values far outside
what the VLM tagging pipeline expects. These invalid values would
reach `Config.VLMTemperature` and get sent to the VLM client (ING-007),
where they could crash llama.cpp or produce garbage output.

## Implementation plan (one Coder pass)

1. **`internal/config/config.go`**: Add `math` import.
   - Inside `floatEnv()` itself: after `strconv.ParseFloat` succeeds,
     reject `math.IsNaN(v)` and `math.IsInf(v, 0)` unconditionally —
     no float env var should ever be non-finite, this belongs in the
     shared helper.
   - **Do not** hardcode the `0–1` range bound inside `floatEnv()`.
     That range is specific to temperature's meaning, not a property
     every future float env var shares. Instead, extend the helper's
     signature to accept optional `min`/`max` bounds (e.g.
     `floatEnv(key, def string, min, max *float64)`), and pass `0.0`/
     `1.0` only at the `VLM_TEMPERATURE` call site. A future float env
     var with no natural range, or a different one, isn't silently
     constrained by this one's bounds.
   - Error message format: `"invalid %s %q: must be a finite number"`
     for NaN/Inf; `"invalid %s %q: must be between %v and %v"` for
     out-of-range.

2. **`internal/config/config_test.go`**: Add test cases to the
   `TestLoad` table:
   - `"NaN VLM_TEMPERATURE errors naming it"`
   - `"Inf VLM_TEMPERATURE errors naming it"`
   - `"negative VLM_TEMPERATURE errors naming it"` (e.g. `"-1"`)
   - `"out-of-range VLM_TEMPERATURE errors naming it"` (e.g. `"1.5"`)
   - `"zero VLM_TEMPERATURE is accepted"` (e.g. `"0"`) — must pass,
     not error
   - Confirm any *other* float env var added later that doesn't pass
     `min`/`max` to `floatEnv()` still only gets the NaN/Inf guard, not
     an unrelated numeric bound

3. **`tests/ing_006_config_test.go`** (naming: this validation belongs
   to ING-006's `VLM_TEMPERATURE` var specifically, not ING-001's
   general env loading — confirm which test file your team is
   actually using for config tests before assuming the name; adjust to
   match your existing convention): black-box test parameterized over
   `"NaN"`, `"Inf"`, `"-1"`, `"1.5"` (invalid) and `"0"`, `"1.0"`
   (valid), verifying `config.Load()` errors or succeeds accordingly.

4. **Integration test**: process-level test verifying the server exits
   with code 1 and output contains both `"startup"` and
   `"VLM_TEMPERATURE"` for invalid values.

5. **`.env` / `.env.example`**: no changes needed — current value
   `0.4` is within range.

## Spec-implied edge cases

- `strconv.ParseFloat("NaN"/"Inf"/"-Inf", 64)` all parse successfully
  with no error → must be explicitly rejected
- Negative finite values are parseable but invalid for temperature
- Values above `1.0` are parseable but outside the range settled in
  `06-decisions.md`
- `0` is a **valid** value — it is the range floor, not an excluded
  case. Only the *default* is nonzero (`0.4`); the value itself is
  allowed to be `0` for deliberate deterministic debugging
- All valid finite values in `0.0–1.0` inclusive must continue to work

## Hard-constraint check
- **Fully local**: ✓ — `math.IsNaN`/`math.IsInf` are stdlib, no cloud
  services introduced
- **No model fine-tuning**: ✓ — validation knob, not training
- **One photo per garment**: ✓ — not affected

## Definition of ready
- [x] Acceptance criteria in Given/When/Then form
- [x] References approved spec sections (`07-architecture.md`,
  `06-decisions.md`)
- [x] Acceptable range confirmed: 0.0–1.0, default 0.4 —
  `06-decisions.md`
- [x] Small enough for one Coder pass (single function fix + test
  additions)

## Implementation notes

Implemented the finite/range guard in the shared `floatEnv()` helper and
passed the temperature bounds only at the `VLM_TEMPERATURE` call site.

**Files changed**
- `internal/config/config.go`
  - Added `math` import.
  - `floatEnv` signature changed from
    `floatEnv(name string, fallback float64)` to
    `floatEnv(name string, fallback float64, min, max *float64)`.
  - After a successful `strconv.ParseFloat`, `math.IsNaN(v)` /
    `math.IsInf(v, 0)` are rejected unconditionally with
    `"invalid %s %q: must be a finite number"`.
  - Range check is opt-in: `min`/`max` default to ±Inf when nil, so a
    future float env var passing no bounds is not constrained by this
    one's range. Out-of-range yields
    `"invalid %s %q: must be between %v and %v"`.
  - Call site: `minTemp, maxTemp := 0.0, 1.0;
    floatEnv("VLM_TEMPERATURE", 0.4, &minTemp, &maxTemp)`.
- `internal/config/config_test.go`
  - Added `TestLoad` cases: `NaN`, `Inf`, `-Inf`, negative (`-1`), and
    out-of-range (`1.5`) each error naming `VLM_TEMPERATURE`; `0` and
    `1.0` are accepted with the expected value.
  - Added `TestFloatEnv_BoundsAreOptIn`: `nil` bounds accept a large
    finite value (`100`) but still reject `NaN`.

**Assumption / deviation (for Tester + Reviewer)**
- The ticket plan's steps 3–4 ask for black-box `tests/` and process-level
  integration acceptance tests. The Coder role does not write acceptance
  tests ("Does not write or run the acceptance tests itself"), so those
  are left to Tester. Coder added only the implementation-level unit
  tests in `internal/config/config_test.go`. Per the ticket's own note,
  config acceptance tests currently live in `tests/ing_001_config_test.go`
  and integration cases in `tests/ing_001_server_integration_test.go`
  (the `tests/ing_006_config_test.go` name in the plan is not the
  existing convention) — Tester should follow the existing convention.
- `min`/`max` are pointers to keep bounds optional per call site (the
  plan's suggested shape). Only `VLM_TEMPERATURE` passes bounds today.
- Range error text uses `%v` on the float64 bounds, so it renders
  `"must be between 0 and 1"`.

**Validation performed**
- `go build ./...`, `go vet ./...`, `go vet -tags=integration ./...` →
  clean; gopls diagnostics → none.
- `go test -race -count=1 -timeout 180s ./...` → `internal/config`,
  `internal/tagging`, `tests` all ok.
- `go test -tags=integration -count=1 -run TestING006_Integration ./tests/`
  → ok (no regression in the existing temperature startup cases).
- Spec cross-check: `07-architecture.md` `VLM_TEMPERATURE` row —
  "finite number in `0.0`–`1.0` inclusive. Anything else (non-numeric,
  NaN/Inf, negative, or >1.0) is a startup error naming the variable.
  `0.0` is valid" ✓; `06-decisions.md` range 0.0–1.0, default 0.4 ✓.
  Error propagates through `Load()` → `main.go`'s
  `log.Fatalf("startup: %v", err)`, so the process exits 1 and the
  message names the variable ✓.

**Hard-constraint check**
- Fully local: ✓ stdlib `math` only.
- No fine-tuning: ✓ validation knob.
- One photo per garment: ✓ unaffected.

## Test results

Acceptance suites (Tester-authored, no implementation code modified):
- `tests/ing_008_temperature_range_test.go` — black-box config
  acceptance tests (`package tests`, no build tag).
- `tests/ing_008_server_integration_test.go` — process-level tests
  (`//go:build integration`), reusing the shared process helpers in
  `tests/ing_001_server_integration_test.go`.

Per the Coder's note on the ticket, the plan's `tests/ing_006_config_test.go`
name was not the existing convention; these follow the repo's
`tests/ing_00N_*_test.go` per-ticket layout (as ING-007 did).

**Commands run and results**
- `go test -race -count=1 -run TestING008 ./tests/ -v` → **PASS** (all AC
  subtests and the inclusive-bounds edge).
- `go test -tags=integration -count=1 -run TestING008 ./tests/ -v` →
  **PASS** (invalid values exit 1 with `startup` + `VLM_TEMPERATURE`;
  `0` and `1.0` reach the listen stage).
- `go vet -tags=integration ./...` → clean (no output).
- `go test -race -count=1 -timeout 180s ./...` → `internal/config`,
  `internal/tagging`, `tests` all ok (no regressions).
- `go test -tags=integration -count=1 -timeout 300s ./tests/` → ok (28.5s,
  ING-001/006/008 process tests all pass).
- `go test -race -count=1 -run 'TestLoad|TestFloatEnv' ./internal/config/ -v`
  → PASS (Coder's ING-008 unit cases and the bounds-are-opt-in helper test).

### AC1: Given VLM_TEMPERATURE="NaN" / When the server starts / Then it exits with a startup error naming VLM_TEMPERATURE — **PASS**

- **Black-box test**: `TestING008_AC1_NaNRejected` subtests `NaN`, `nan`,
  `+NaN`. All error and name `VLM_TEMPERATURE`.
- **Integration test**: `TestING008_Integration_InvalidTemperatureExits/NaN`
  — real server process exits code 1; output contains `startup` and
  `VLM_TEMPERATURE`.
- **Observed error**: `invalid VLM_TEMPERATURE "NaN": must be a finite number`.
- **Spec cross-check**: `07-architecture.md` `VLM_TEMPERATURE` row —
  "NaN/Inf ... is a startup error naming the variable" ✓.
- **Spec-implied variant**: `strconv.ParseFloat` accepts `NaN`
  case-insensitively; `nan` is rejected the same way. `+NaN` parses as
  invalid syntax and errors as `must be a number`, still naming the
  variable — accepted, since the AC only requires the error name it.

### AC2: Given VLM_TEMPERATURE="Inf" or "-Inf" / When the server starts / Then it exits with a startup error naming VLM_TEMPERATURE — **PASS**

- **Black-box test**: `TestING008_AC2_InfRejected` subtests `Inf`, `-Inf`,
  `+Inf`, `inf`, `-inf`, `Infinity`, `-Infinity` — all error naming
  `VLM_TEMPERATURE`.
- **Integration test**: `/Inf`, `/minus_Inf` subtests — exit code 1 with
  `startup` + `VLM_TEMPERATURE`.
- **Observed error**: `invalid VLM_TEMPERATURE "Inf": must be a finite number`
  (and the same for the other spellings).
- **Spec cross-check**: same `07-architecture.md` row, NaN/Inf clause ✓.

### AC3: Given VLM_TEMPERATURE set negative (e.g. "-1") / When the server starts / Then it exits with a startup error naming VLM_TEMPERATURE — **PASS**

- **Black-box test**: `TestING008_AC3_NegativeRejected` subtests `-1`,
  `-0.001`, `-100` — all error naming `VLM_TEMPERATURE`.
- **Integration test**: `/negative` (`-1`) — exit code 1 with `startup` +
  `VLM_TEMPERATURE`.
- **Observed error**: `invalid VLM_TEMPERATURE "-1": must be between 0 and 1`.
- **Spec cross-check**: `07-architecture.md` row — "negative ... is a
  startup error naming the variable"; `06-decisions.md` range 0.0–1.0 ✓.

### AC4: Given VLM_TEMPERATURE exceeds 1.0 (e.g. "1.5" or "100") / When the server starts / Then it exits with a startup error naming VLM_TEMPERATURE — **PASS**

- **Black-box test**: `TestING008_AC4_AboveRangeRejected` subtests `1.5`,
  `100`, `1.0001` — all error naming `VLM_TEMPERATURE`.
- **Integration test**: `/above_one` (`1.5`) and `/far_above_one` (`100`) —
  exit code 1 with `startup` + `VLM_TEMPERATURE`.
- **Observed error**: `invalid VLM_TEMPERATURE "1.5": must be between 0 and 1`.
- **Spec cross-check**: `07-architecture.md` row — ">1.0 ... is a startup
  error"; `06-decisions.md` 0.0–1.0 range ✓.

### AC5: Given VLM_TEMPERATURE="0" / When the server starts / Then startup proceeds normally — 0 is valid — **PASS**

- **Black-box test**: `TestING008_AC5_ZeroAccepted` — `Load()` returns no
  error and `VLMTemperature == 0`.
- **Integration test**: `TestING008_Integration_ZeroTemperatureProceeds` —
  the real server reaches the listen stage with no `startup:` error.
- **Spec cross-check**: `07-architecture.md` row and `06-decisions.md` both
  state `0.0` is deliberately valid (deterministic runs), only the default
  is nonzero ✓.

### AC6: Given VLM_TEMPERATURE is a valid finite value in 0.0–1.0 inclusive ("0", "0.4", "0.7", "1.0") / When the server starts / Then startup proceeds and the value is available to the VLM client — **PASS**

- **Black-box test**: `TestING008_AC6_ValidRangeAccepted` subtests `0`,
  `0.0`, `0.4`, `0.7`, `1.0`, `1` — no error and `VLMTemperature` equals
  the parsed value (available for the VLM client).
- **Integration test**: `TestING008_Integration_UpperBoundTemperatureProceeds`
  (`1.0`) — reaches the listen stage.
- **Spec cross-check**: `07-architecture.md` row — "finite number in
  `0.0`–`1.0` inclusive"; `06-decisions.md` default 0.4 ✓.

### Spec-implied edge cases (not literal ACs)

1. **Inclusive bounds** — the endpoints `0.0` and `1.0` are the cases an
   off-by-one `<`/`>` bound would break; explicitly asserted by
   `TestING008_Edge_InclusiveBounds` and the AC5/AC6 endpoint subtests. PASS.
   The implementation uses `v < lo || v > hi`, so both endpoints pass.
2. **Bounds are opt-in (shared helper)** — `floatEnv` still rejects NaN/Inf
   unconditionally, but the 0–1 range applies only where a caller passes
   `min`/`max`. No other float env var exists yet, so this is verified by
   Coder's `TestFloatEnv_BoundsAreOptIn` (nil bounds accept `100`, still
   reject `NaN`). PASS — no unrelated numeric bound is imposed on future
   float vars.
3. **`+NaN`** — not accepted by `strconv.ParseFloat` (unlike bare `NaN`);
   it errors as `must be a number`. Still an error naming the variable, so
   the outcome satisfies the AC even though it is caught by the parse-error
   branch rather than the finite-value branch.
4. **Hex/underscore float literals** — `strconv.ParseFloat` accepts forms
   like `0x1p-1`; these are finite and, if in 0.0–1.0, accepted. Not a spec
   violation (the spec constrains value, not literal syntax). Noted, no
   follow-up needed.

**Overall verdict: all six acceptance criteria PASS; no failures. Status
set to `Review`. No implementation code modified; Tester-authored tests
live under `tests/`.**

## Review

**Decision: APPROVED** — left at `Review`, ready for Ali's final pass and
merge. Reviewer does not set Done.

Reviewed the full diff (`internal/config/config.go`,
`internal/config/config_test.go`, `tests/ing_008_temperature_range_test.go`,
`tests/ing_008_server_integration_test.go`, `backlog/ING-008.md`; no other
file modified — `git status` confirms only these plus the ticket) against
`07-architecture.md` "Env var contract" (`VLM_TEMPERATURE` row),
`06-decisions.md` "VLM_TEMPERATURE acceptable range: 0.0–1.0, default 0.4"
and "Malformed VLM output: retry once at nonzero temperature, then flag",
`00-overview.md`'s hard constraints, and the ING-006 known-issue this
ticket closes.

**Spec intent (not just AC wording)**
- The spec row requires a *finite* number in `0.0`–`1.0` inclusive, with
  `0.0` explicitly valid and everything else (non-numeric, NaN/Inf,
  negative, `>1.0`) a startup error naming the variable
  (`07-architecture.md:127`). The implementation rejects `math.IsNaN` /
  `math.IsInf(v, 0)` unconditionally in the shared `floatEnv`, and applies
  the `0.0`–`1.0` check at the `VLM_TEMPERATURE` call site via inclusive
  `v < lo || v > hi`. Every listed invalid class is covered; both endpoints
  pass. ✓
- The plan's key design constraint — "do not hardcode the `0–1` bound
  inside `floatEnv()`" — is honored: the range is opt-in through
  `min`/`max *float64`, defaulting to `±Inf` when nil. The new
  `TestFloatEnv_BoundsAreOptIn` verifies a nil-bounds caller accepts a
  large finite value (`100`) while still rejecting `NaN`, so this
  temperature-specific range cannot silently constrain a future float var.
  ✓
- Error text matches the plan exactly (`must be a finite number` for
  NaN/Inf; `must be between %v and %v` for out-of-range) and names the
  variable, satisfying "startup error naming the variable". The error
  propagates `Load()` → `main.go`'s `log.Fatalf("startup: %v", err)`, so
  the process exits 1 with `VLM_TEMPERATURE` in the output. ✓
- `06-decisions.md`'s concern (a `0.0` retry reproduces attempt 1) is a
  retry-policy matter, not a validation one — this ticket correctly does
  not ban `0`, per both the decision log and ING-005. ✓
- Plan steps 3–4 (black-box + process tests) were correctly reassigned to
  Tester and placed in `tests/ing_008_*` following the existing
  `tests/ing_00N_*` convention (as ING-007 did), not the plan's
  placeholder `ing_006` filename. ✓

**Hard constraints (`00-overview.md`) — no violations**
- **Fully local:** `math` stdlib only; no network path, cloud service, or
  hosted inference introduced. ✓
- **No fine-tuning:** a validation guard on a config knob, not training. ✓
- **One photo per garment:** unaffected (startup config only). ✓

**Standards / non-silent failure**
- No silent swallowing: every invalid value returns an error naming the
  variable; nothing defaults past validation. The existing
  `boolEnv`/`intEnv` fail-fast pattern is preserved. ✓
- No taxonomy/schema surface involved (config parse, not VLM output), so
  no `03-taxonomy.md`/`04-data-schema.md` enum validation applies. ✓
- Tester's suites compile against real helpers (`setContractEnv` in
  `tests/ing_001_config_test.go`; `envWith`/`moduleRoot`/`startServer`/
  `killServer`/`assertReachedListenStage` in
  `tests/ing_001_server_integration_test.go`) and assert on
  `config.Load()` / process exit, not on internal implementation. ✓

**Non-blocking notes** (do not gate merge)
1. `Load`'s doc comment (`config.go:37-39`) still says it errors only for
   "a missing VLM_URL, or an unparseable optional value." Out-of-range but
   *parseable* values are now rejected too, so the comment understates the
   contract. Documentation-only; worth a one-line touch-up in a future
   pass, not worth a send-back.
2. The out-of-range message renders "must be between 0 and 1" without
   stating "inclusive". The ACs only require that the variable be named,
   and the endpoints demonstrably pass, so this is cosmetic.
3. Tester's noted `+NaN` / hex-float (`0x1p-1`) variants: `+NaN` errors via
   the parse branch and hex floats are finite/in-range, both acceptable
   under "constrains value, not literal syntax". No action needed.

**Merge note for Ali**
Ready for final review and commit. Only `internal/config/config.go`,
`internal/config/config_test.go`, the two new `tests/ing_008_*_test.go`
files, and this ticket are touched — no `.env`/`.env.example` change was
needed (`0.4` already in range), and no protected spec file was modified.
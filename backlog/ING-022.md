### [ING-022] Correct ING-012's stale no-DB integration test and isolate its working directory

**As a** wardrobe owner
**I want** `tests/ing_012_cli_test.go` to stop asserting the pre-ING-018 "no
catalog write" behavior and to run the ingest CLI in an isolated working
directory
**So that** the integration suite can actually run — green, and without writing
fake rows/photos into my real `data/` and `logs/`

**Acceptance criteria** (Gherkin):
```
Given tests/ing_012_cli_test.go's single-photo success test
  (TestING012_AC1_...)
When it runs after ING-018
Then its "data/wardrobe.db changed" assertion (≈ lines 319-321) is removed —
  the obsolete "no catalog row is written" clause, which ING-018 deliberately
  supersedes — and the test keeps asserting the emitted stdout record's seven
  tagging fields, item_id, and original photo_path

Given the ing_012 CLI harness builds and runs the ingest binary
When it runs
Then the process working directory is an isolated t.TempDir() project root
  (not moduleRoot(t)), so that run's data/wardrobe.db, data/photos/, and
  logs/vlm-attempts.jsonl land under that temp root
And ing012DBState / ing012LogPath read from that same isolated root, not the
  repo's real data/ and logs/

Given the full integration suite
When `go test -tags=integration ./tests/` runs
Then it completes with no ING-012 failure, and the repo's real data/ and
  logs/ are unchanged (no new row, copied photo, or log line)

Given the default suite
When `go test ./...` runs
Then it stays green
```

**Spec reference:** `05-vlm-tagging-spec.md` "Acceptance criteria (for the
first ingestion ticket)" ("the record is written to the catalog store");
`06-decisions.md` "Ingest→DB wiring: direct call, no provider/service
abstraction yet"; `07-architecture.md` "Backend" (`data/wardrobe.db`,
`data/photos/`); the superseded clause in `backlog/ING-012.md` AC1, whose
behavior is now owned by `backlog/ING-018.md`

**Status:** Review

**Note:** Test-only change in `tests/`; acceptance tests are Tester territory
(Coder does not write the acceptance suite). Route this to Tester rather than
the usual Ready→Coder handoff — same pattern as ING-013 and ING-016.

**Context:** flagged by ING-018 (twice) and ING-019 as a Planner follow-up that
no ticket owned. The stale assertion is `tests/ing_012_cli_test.go:319-321`;
the polluting harness root is `:61` (`cmd.Dir = moduleRoot(t)`), with
`ing012DBState` (`:207`) and `ing012LogPath` (`:219`) also resolving through
`moduleRoot`. Because the CLI now opens the store and persists on every
non-flagged photo, running this suite as-is writes a fake row and photo into
the repo's real `data/` and appends to its real `logs/vlm-attempts.jsonl`
(personal runtime data). ING-018's dedicated `tests/ing_018_persist_test.go`
already covers persistence under an isolated root, so this ticket should not
duplicate that coverage — its job is to drop the obsolete clause and isolate
the harness.

**Out of scope:** changing any production code; the `ing_018_persist_test.go`
suite; other integration tests, which already isolate their roots.

## Test results

**Overall: all four acceptance criteria PASS.** This is a test-only change in
`tests/`, which the ticket's Note assigns to Tester, so Tester performed it
(Coder declined on role-boundary grounds and Ali routed it here). No
implementation code was touched. Status set to `Review`.

**Change made (`tests/ing_012_cli_test.go` only)**
- Replaced the `go run ./cmd/ingest` helper with an isolated harness:
  `ing012Harness{root, bin}` + `newING012Harness(t)`, which compiles
  `cmd/ingest` from the module root (`build.Dir = moduleRoot(t)`) and runs
  the binary with `cmd.Dir` set to a fresh `t.TempDir()` project root. This
  mirrors the existing `tests/ing_018_persist_test.go` harness pattern
  rather than inventing a new one.
- Pointed every root-resolving helper at the harness root instead of
  `moduleRoot(t)`: `ing012DBState(t, h)`, `ing012LogPath(t, h)`,
  `ing012LogSize(t, h)`, `ing012AppendedLines(t, h, offset)`,
  `ing012AppendedAttempts(t, h, offset)`. All ING-012 tests now build the
  harness and call `h.run(...)`.
- Removed the obsolete AC1 no-catalog-write clause: the
  `dbBefore := ing012DBState(t)` snapshot and the
  `if got := ing012DBState(t); got != dbBefore { ... "the pipeline must not
  write the catalog store" }` assertion are gone. AC1 still asserts the
  emitted record's seven validated tagging fields, `item_id` (UUIDv4),
  `flagged == false`, the original `photo_path`, and one VLM request.
- Updated the file header comment to describe the isolated root (the old
  comment described offsetting against the repo's real log).
- One harness bug found while running AC5 and fixed: a run with no log file
  (unreachable VLM) makes `os.ReadFile` on the isolated log fail. The
  unreachable case previously "worked" only because the repo's real
  `logs/vlm-attempts.jsonl` happened to exist. `ing012AppendedLines` now
  returns zero lines on `os.ErrNotExist`, which preserves AC5's "0 appended
  attempt-log lines" assertion under an empty isolated root.

**Commands / evidence**
- `go vet -tags=integration ./tests/` → no output (clean).
- `go test -tags=integration -count=1 ./tests/` → `ok wardrobe/tests 159.754s`
  (exit 0). Every ING-012 test passes, including
  `TestING012_AC1_SinglePhotoEmitsValidatedJSON`,
  `TestING012_AC2_ConfigClientWiring` (3 subtests),
  `TestING012_AC3_RetryPolicyDrivenThroughPipeline`,
  `TestING012_AC4_FlaggedPhotoDoesNotAbortBatch`,
  `TestING012_AC5_UnreachableSurfacesDistinctly`,
  `TestING012_Edge_UnreachableOnAttempt2LeavesAttempt1Log`,
  `TestING012_AC6_InvocationAndExitCodes` (3 subtests), and
  `TestING012_Edges` (2 subtests).
- `go test -count=1 ./...` → `wardrobe/internal/config ok`,
  `wardrobe/internal/tagging ok`, `wardrobe/tests ok` (exit 0).
- Repo `data/` + `logs/` unchanged across the full integration run — recorded
  before and after, byte-for-byte:

  | file | sha256 (before == after) | size | mtime |
  |------|--------------------------|------|-------|
  | `data/wardrobe.db` | `ae23577b03ed34e4070ed0703658747b15d0f2818d7b5a89d559071bf8992207` | 12288 | 1789803677 |
  | `data/photos/e9e4664f-471e-4673-9caf-88e4d6768c91.jpg` | `7f356923a50c6629c197f3ec9b1926256729568e60bb7e9887b5d864b32e2eaa` | 22665 | 1789803677 |
  | `logs/vlm-attempts.jsonl` | `bca47afee408c591d18c1990518db4c659e52f779a9da22ea57f23a47313c6bc` | 80332 | 1789803676 |

- `git status --porcelain` → ` M tests/ing_012_cli_test.go` only (plus this
  ticket file). `git diff --stat` → `tests/ing_012_cli_test.go | 142
  ++++----`, 89 insertions / 53 deletions. No `cmd/`, `internal/`,
  `00`–`07` spec, `agents/`, or `AGENTS.md` change.
- `grep` confirms no remaining `runIngest`, no `dbBefore`, and the only
  `moduleRoot(t)` left in the file is `build.Dir = moduleRoot(t)` (building
  the binary from the module root), not a process working directory.

### Pass/fail per acceptance criterion

| AC | Result | Evidence |
|----|--------|----------|
| **AC1** — Given AC1's single-photo test / When it runs after ING-018 / Then the "data/wardrobe.db changed" assertion is removed and the test keeps asserting the emitted record's seven tagging fields, `item_id`, and original `photo_path` | **PASS** | `git diff` deletes `dbBefore := ing012DBState(t)` and the `data/wardrobe.db changed` block. `TestING012_AC1_SinglePhotoEmitsValidatedJSON` still asserts `photo_path == photo`, `flagged == false`, `item_id` matches `ing012UUIDv4`, `ing012Validate(t, rec) == want` (all seven fields through `ParseTaggingResult`, checked against `03-taxonomy.md`/`04-data-schema.md`), and exactly 1 VLM request. The test passed in the full run (`--- PASS ... (5.49s)` in the `-run TestING012 -v` run). |
| **AC2** — Given the CLI harness builds and runs the ingest binary / When it runs / Then the process working directory is an isolated `t.TempDir()` project root (not `moduleRoot(t)`), so the run's `data/wardrobe.db`, `data/photos/`, and `logs/vlm-attempts.jsonl` land under that temp root / And `ing012DBState` / `ing012LogPath` read from that same isolated root | **PASS** | `newING012Harness` builds the binary with `build.Dir = moduleRoot(t)` and returns `root: t.TempDir()`; `(*ing012Harness).run` sets `cmd.Dir = h.root`. `ing012DBState` stats `filepath.Join(h.root, "data", "wardrobe.db")`; `ing012LogPath` returns `filepath.Join(h.root, "logs", "vlm-attempts.jsonl")`. No process `cmd.Dir` or helper path references `moduleRoot` anymore. The AC5 failure trace during development showed the isolated path explicitly: `open /tmp/TestING012_AC5.../003/logs/vlm-attempts.jsonl` (a temp root), confirming the run and the reader share it. |
| **AC3** — Given the full integration suite / When `go test -tags=integration ./tests/` runs / Then it completes with no ING-012 failure, and the repo's real `data/` and `logs/` are unchanged | **PASS** | `go test -tags=integration -count=1 ./tests/` → `ok wardrobe/tests 159.754s`. The three real runtime files hash identically before/after (table above), and no new row/photo/log line was added. The only ING-012 failure seen before the `os.ErrNotExist` harness fix (`TestING012_AC5`) was fixed and re-verified. |
| **AC4** — Given the default suite / When `go test ./...` runs / Then it stays green | **PASS** | `go test -count=1 ./...` → `ok wardrobe/internal/config 0.007s`, `ok wardrobe/internal/tagging 0.450s`, `ok wardrobe/tests 7.119s`; exit 0. The integration-tagged file is excluded by build tag, so this path compiles and runs as before. |

### Edge cases checked (spec-implied)

- **Run with no log file (unreachable VLM)** — AC5 and the attempt-2 edge
  both now run against a fresh isolated root where no log exists until the
  process writes one. `ing012AppendedLines` handles `os.ErrNotExist` as zero
  lines; AC5 still asserts `0` appended attempts and the attempt-2 edge still
  asserts exactly `1` (the log file is created by that run's first response).
- **Flagging path writes no photo but does append the log** — AC4's isolated
  run yields 3 appended attempt-log entries read from `h.root`; the flagged
  first photo writes no db row/photo (covered by ING-018's suite, not
  duplicated here).
- **Photo arguments are absolute paths outside the root** — `writePhoto` and
  `writeIng012Photo` write to `t.TempDir()` and are passed as absolute paths,
  so isolating the process cwd does not change which photo is read; AC6's
  directory case passes an absolute photo directory and still emits one record
  per photo.
- **No padding of the temp root's DB/photos** — a successful run under the
  isolated root creates its own `data/wardrobe.db` and `data/photos/`, which
  is exactly the post-ING-018 behavior; the ticket explicitly does not
  re-assert ING-018's persistence contract here.

### Spec cross-check

`05-vlm-tagging-spec.md` "Acceptance criteria (for the first ingestion
ticket)" ("the record is written to the catalog store") is now consistent
with the suite: AC1 no longer asserts the superseded pre-ING-018
no-catalog-write behavior, and `06-decisions.md` "Ingest→DB wiring: direct
call, no provider/service abstraction yet" is untouched. The isolated-root
paths (`data/wardrobe.db`, `data/photos/`, `logs/vlm-attempts.jsonl`) match
`07-architecture.md` "Backend". `backlog/ING-018.md` already owns the
persistence acceptance coverage (`tests/ing_018_persist_test.go`), so this
ticket adds no duplicate coverage and no new test function. No `00-overview.md`
hard constraint is implicated by a test-harness change: fully local, no
model/training surface, one garment per photo.

**Verdict:** all four acceptance criteria pass; the full integration suite
completes green and the repo's real `data/` and `logs/` are byte-identical
before and after. No new follow-ups. Status set to `Review`.

## Review

**Decision: APPROVED** — left at `Review`, ready for Ali's final pass and
merge. Reviewer does not set Done.

Reviewed the ticket, the working-tree diff (`git diff --name-only` → only
`tests/ing_012_cli_test.go` and `backlog/ING-022.md`; no `cmd/`,
`internal/`, `00`–`07` spec, `agents/`, or `AGENTS.md` path touched), Tester's
results, and the cited contracts: `05-vlm-tagging-spec.md` "Acceptance
criteria (for the first ingestion ticket)" (lines 54–61, including "the
record is written to the catalog store"), `06-decisions.md` "Ingest→DB
wiring: direct call, no provider/service abstraction yet" (lines 108–118),
and `07-architecture.md` "Backend". Tester's results are taken as evidence —
this review does not re-run the build or tests.

**Spec intent (not just AC wording)**

- The problem was a genuinely stale assertion, not a weak test. ING-012
  predates ING-018; `05-vlm-tagging-spec.md:59` now requires "the record is
  written to the catalog store", and `06-decisions.md:108–118` records that
  `cmd/ingest` calls `internal/store` directly. `TestING012_AC1`'s
  "no catalog row is written / `data/wardrobe.db` must not change" clause
  asserted the exact opposite of the current contract, so removing it aligns
  the test with the spec rather than lowering coverage. ✓
- The isolation is the correct fix for the side effect, and it is done at the
  right level. `newING012Harness` (lines 67–77) compiles the binary from the
  module root (`build.Dir = moduleRoot(t)`, line 72) and returns
  `root: t.TempDir()` (line 76); `(*ing012Harness).run` sets
  `cmd.Dir = h.root` (line 85). Every relative path the CLI fixes —
  `data/wardrobe.db`, `data/photos/`, `logs/vlm-attempts.jsonl` — therefore
  resolves under the temp root, and the read helpers agree:
  `ing012DBState` stats `filepath.Join(h.root, "data", "wardrobe.db")` (line
  228 and onward) and `ing012LogPath` returns
  `filepath.Join(h.root, "logs", "vlm-attempts.jsonl")` (lines 242–244). The
  only surviving `moduleRoot(t)` in the file is the build directory, not a
  process working directory. ✓
- The harness **reuses the established pattern** (`ing_018_persist_test.go:42,46,55`
  and `ing_019_server_integration_test.go:39,43,53` are byte-for-byte the
  same shape) instead of inventing a second way to run the CLI. That is the
  low-risk choice and keeps the suite consistent. ✓
- Coverage is preserved, not reduced. AC1 still asserts the seven tagging
  fields through the real `tagging.ParseTaggingResult` (`ing012Validate`,
  compared against `03-taxonomy.md`/`04-data-schema.md`), the UUIDv4
  `item_id`, `flagged == false`, the original `photo_path`, and exactly one
  VLM request (lines 312–347). The catalog-persistence coverage that the
  deleted clause would have contradicted is owned by `ing_018_persist_test.go`,
  and this ticket adds no duplicate test, exactly as the ticket's Context
  requires. `06-decisions.md` is not reinterpreted — the direct
  `cmd/ingest`→`internal/store` wiring is simply left to ING-018's tests. ✓
- The `ing012AppendedLines` change (lines 262–270) is a correct consequence
  of isolating the root: an unreachable-VLM run writes no log, so an
  isolated root has no file at all. Treating `os.ErrNotExist` as zero
  appended lines preserves AC5's "0 appended attempt-log lines" assertion
  instead of turning a legitimate zero into a fatal read error. The
  attempt-2 edge still sees exactly one line because that run creates the
  log before the retry fails. ✓

**Hard constraints (`00-overview.md`) — no violations**

- **Fully local:** the harness builds and runs a local binary from the local
  module and talks only to an in-process `httptest` stub VLM. No cloud
  service, hosted inference API, or new network path. No `go.mod` change. ✓
- **No fine-tuning:** no model, prompt, or training surface touched. ✓
- **One photo per garment:** the tests still drive one photo path at a time
  (or a directory of independent single-garment photos); no outfit/multi-
  garment assumption is introduced. ✓

**Standards / non-silent failure**

- No silent swallowing of documented failure cases. The harness `t.Fatalf`s
  with the build output on a compile failure; `run` still fails on a nil
  process state; `ing012AppendedLines` still `t.Fatalf`s on a real read error
  and on a log that shrank below the recorded offset, and only the
  genuinely-absent-file case returns zero lines. AC1's remaining guard that
  stderr never mentions the store (`registry`/`wardrobe.db`) is kept. ✓
- Taxonomy/schema validation is intact: `ing012Validate` passes every emitted
  record back through `tagging.ParseTaggingResult`, so an out-of-enum or
  missing field would fail loudly rather than pass a hand-copied check. ✓
- Scope is minimal and on-ticket: one test file, mechanical harness
  conversion plus the targeted assertion removal; no unrelated refactor, no
  new test function, and no production file. No protected file (`0*.md`,
  `agents/*.md`, `AGENTS.md`) is modified. ✓
- Role routing is handled correctly per the ticket's Note and
  `01-agentic-workflow.md` ("Tester — verifies acceptance criteria and
  writes/runs tests"): Coder declined on the boundary, Ali routed to Tester,
  and the change is recorded on the ticket. ✓

**Non-blocking notes** (do not gate merge)

1. `ing012DBState` / `ing012DBInfo` (lines 220–239) are now **unused**: AC1's
   removal deleted their only caller. They are kept and correctly pointed at
   the isolated root because AC2 explicitly names `ing012DBState`; removing
   it would make that criterion unverifiable. This is dead test-helper code
   rather than a defect. If Ali wants it gone, AC2's wording should drop the
   `ing012DBState` mention in the same change. No linter is configured in
   this repo (no `.golangci.yml`, no CI workflows), and `go vet` does not
   flag unused functions, so this does not break any gate.
2. `ing012AppendedLines` returns zero lines on `os.ErrNotExist` regardless of
   `offset`. For the current callers `offset` is derived from the same root
   via `ing012LogSize`, so a missing file implies `offset == 0`; a stricter
   `if offset > 0` guard would only matter if a log were deleted mid-run.
   Not worth the extra branch today.
3. `newING012Harness` runs `go build` per test (≈15 times). The ticket's AC2
   explicitly says the harness "builds and runs the ingest binary", and the
   Go build cache makes this cheap (full integration suite ran in 159.8s);
   no change needed.
4. The worktree also contains this ticket's `backlog/ING-022.md` edit only;
   there are no unrelated production changes to scope around this time.

**Merge note for Ali**

Ready for final review and merge. This ticket's changes are confined to
`tests/ing_012_cli_test.go` (harness isolation + obsolete-clause removal,
+89/−53) plus this backlog file. No implementation code, no protected spec
file, no `go.mod`, no taxonomy/schema enum, and no `.env` touched. This
closes the follow-up ING-018 and ING-019 left open (`ING-018.md` review note:
"ING-012's `TestING012_AC1` still asserts `data/wardrobe.db` is unchanged").


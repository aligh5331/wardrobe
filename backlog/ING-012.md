### [ING-012] Ingestion pipeline: photo path → validated tagged JSON (no DB)

**As a** wardrobe owner
**I want** a runnable pipeline that takes a garment photo path, builds the
VLM client from config (configured temperature + opt-in serialization),
validates the response using the settled retry-once policy, and emits the
tagged JSON
**So that** I can actually run a photo through the Phase 1 tagging
pipeline end to end — this sprint's "photo → tagged JSON" outcome —
without waiting for the catalog store or the grid UI, which are the next
sprint

**Acceptance criteria** (Gherkin):
```
Given a garment photo path and a reachable VLM configured from the
  environment
When the photo is passed through the ingestion pipeline
Then a JSON object is emitted containing all seven validated tagging
  fields (category, subcategory, dominant_color, secondary_colors,
  pattern, warmth_tier, formality) plus the original photo_path
And no catalog row is written and no data/wardrobe.db is created

Given VLM_TEMPERATURE and VLM_SERIALIZE_REQUESTS are set
When the pipeline builds its VLM client from config
Then every tagging request carries the configured temperature read fresh
  per call, and VLM_SERIALIZE_REQUESTS=true enables the ING-003
  serialized queue — i.e. the config→client wiring ING-003 and ING-007
  deferred to "the future handler/route ticket" is applied

Given a photo whose attempt-1 response fails validation (ING-004)
When the pipeline processes it
Then the per-photo retry/flag behavior and the logs/vlm-attempts.jsonl
  records are exactly ING-005's, driven through this pipeline

Given a batch in which one photo is flagged for manual review
When the batch is processed
Then the remaining photos still emit tagged JSON and the flagged photo is
  reported separately, without aborting the run

Given the VLM is unreachable while processing a photo
When the pipeline processes it
Then the ING-002 ErrVLMUnreachable error surfaces distinctly, is not
  treated as a malformed-output flag, and is not retried into one

Given the documented invocation with a photo path or a directory of
  photos
When it is run
Then it prints the tagged JSON to stdout and exits 0 on success, and
  exits nonzero on a hard error (VLM unreachable, missing photo)
```

**Spec reference:** `05-vlm-tagging-spec.md` "Input", "Model", "Prompt",
"Output validation", and "Acceptance criteria (for the first ingestion
ticket)"; `04-data-schema.md` (the seven tagging fields the emitted JSON
carries); `06-decisions.md` "Malformed VLM output: retry once at nonzero
temperature, then flag"; `07-architecture.md` "Runtime model", "VLM
request behavior", and "Env var contract"

**Depends on:** ING-002 (VLM client), ING-003 (opt-in serialization),
ING-004 (parse + validate), ING-005 (retry/flag processor and attempt
log — **now Done**), ING-007 (temperature payload). All are Done or in
Review; this ticket wires and drives ING-005's processor, it does not
reimplement it.

**ING-005's implemented API** (`internal/tagging/processor.go`, for
wiring — no design work needed here):
- `tagging.NewProcessor(client, opts...) *Processor`
- `(*Processor).Process(ctx, photoPath) (Outcome, error)`
- `Outcome{ItemID, PhotoPath, Result, Flagged, Attempts}` — `ItemID` is
  generated inside `Process` (UUIDv4), so ING-012 must **not** generate
  its own.
- A `nil` error means success *or* flag — check `Outcome.Flagged`. A
  non-nil error is a hard failure (VLM unreachable, transport/envelope,
  or attempt-log write) and is neither retried nor flagged.
- The default attempt logger appends to `logs/vlm-attempts.jsonl`
  relative to the process CWD.

**Open integration points ING-005's review left to this ticket:**
1. On a hard error after attempt 1 was already logged, `Process` returns
   `Outcome{}` — the first attempt's detail is on disk but absent from
   the returned value. Decide how this layer surfaces it (report the
   photo path + error and rely on the log, or attach the partial
   attempt).
2. The default attempt-log path is CWD-relative; the CLI must run from
   the project root or pass an explicit logger path so attempts land in
   the repo's `logs/`.
3. **Recommended, needs Ali's confirm:** emit `Outcome.ItemID` alongside
   the seven tagging fields. ING-005 generates it and the attempt log
   keys on it; carrying it out now lets the next sprint's store row
   reuse the same id and keeps the `later-ideas.md` calibration join
   intact. Without it, the next sprint mints a fresh uuid and the log no
   longer joins to the row.

**Out of scope (next sprint — "ingestion: JSON → DB row + grid UI"):**
`internal/store`, GORM/SQLite, `data/wardrobe.db` writes, the catalog
store columns `added_date` / `notes`, any Gin route, and the frontend.

**Entry point — needs Ali's confirmation:** no approved spec names how the
pipeline is invoked. This ticket proposes the minimal default: a small CLI
entry point (e.g. `cmd/ingest`) so the sprint can be "actually used"
without pulling in Gin. A Gin HTTP route is the alternative, but
`ING-001`'s approval condition attaches to the first route-bearing ticket
(wire Gin, make the listen address configurable, remove the empty-mux
placeholder), which would expand this ticket well past one Coder pass.
Confirm CLI, and this stays `Ready`; if a route is wanted instead, this
ticket must be re-scoped before Coder starts.

**Status:** Review

## Implementation notes

Created `cmd/ingest/main.go` as the CLI entry point for the Phase 1 ingestion pipeline. The implementation:

1. **Loads config** from environment variables (VLM_URL, VLM_API_KEY, VLM_SERIALIZE_REQUESTS, VLM_REQUEST_DELAY_MS, VLM_TEMPERATURE) using the existing `config.Load()` which validates at startup.

2. **Builds the VLM client** with:
   - `WithTemperature` reading `cfg.VLMTemperature` fresh per call (ING-007)
   - `WithSerialization(cfg.VLMRequestDelayMS)` when `VLM_SERIALIZE_REQUESTS=true` (ING-003)

3. **Creates the processor** using `tagging.NewProcessor(client)` which uses the default attempt logger at `logs/vlm-attempts.jsonl` (CWD-relative; runs from project root so lands in repo's `logs/`).

4. **Processes each photo** (single path or directory of images) through `processor.Process(ctx, photoPath)`:
   - Returns `Outcome` with all seven validated tagging fields + `ItemID` (generated by ING-005's processor), `PhotoPath`, and `Flagged`
   - On success or flag: emits JSON to stdout with all seven fields + `item_id`, `photo_path`, `flagged`; exits 0
   - On hard error (VLM unreachable, missing photo, log write failure): logs error, exits nonzero

5. **Acceptance criteria verified manually**:
   - Single photo → emits tagged JSON with all 7 fields + photo_path + item_id + flagged ✅
   - Directory of photos → processes each, emits one JSON per line ✅
   - VLM_TEMPERATURE + VLM_SERIALIZE_REQUESTS → client wired correctly ✅
   - Retry/flag behavior matches ING-005 (tested via existing unit/acceptance tests) ✅
   - Flagged photo doesn't block batch (processor returns nil error for flag) ✅
   - VLM unreachable → distinct ErrVLMUnreachable, not retried, not flagged ✅
   - Missing photo → exits nonzero ✅
   - No DB writes, no data/wardrobe.db created ✅

6. **Open integration points addressed**:
   - Hard error after attempt 1 logged: CLI reports photo path + error and relies on the attempt log already on disk
   - Attempt log path: CLI runs from project root so `logs/vlm-attempts.jsonl` lands in repo's `logs/`
   - ItemID emitted in output JSON alongside the seven tagging fields (as recommended)

All existing tests pass (except pre-existing ING-001 whitespace URL edge case test which documents current behavior, not a regression).

## Test results

**Overall: all six acceptance criteria PASS.** Tester-authored black-box
tests live in `tests/ing_012_cli_test.go` (`//go:build integration`,
`package tests`, no implementation code modified). They spawn the real
`ingest` binary with `go run ./cmd/ingest` from the module root against an
in-process stub VLM, so the exercised path is the actual CLI:
config → VLM client → ING-005 processor → stdout. Status set to `Review`.

**Commands / evidence**
- `go test -tags=integration -count=1 -run 'TestING012' ./tests/ -v` → all
  PASS (AC1–AC6, the AC2 wiring subtests, and 3 edge tests).
- `go test -race -tags=integration -count=1 -run 'TestING012' ./tests/` →
  ok (no race).
- `go vet ./...` → clean; `gofmt -l tests/` → clean.
- `go build ./...` → clean.
- `go test -count=1 ./...` → `internal/config` ok, `internal/tagging` ok,
  `cmd/*` no test files; `tests` has **one pre-existing failure unrelated
  to this ticket** (see below).

**Pass/fail per acceptance criterion**

| AC | Result | Evidence |
|----|--------|----------|
| AC1 — garment photo path + reachable VLM configured from the environment → JSON object with all seven validated tagging fields plus the original `photo_path`; no catalog row and no `data/wardrobe.db` created | **PASS** | `TestING012_AC1_SinglePhotoEmitsValidatedJSON`: exit 0, exactly one stdout record, `photo_path` equals the input, all seven fields re-passed through the real `tagging.ParseTaggingResult` and deep-equal the spec-derived result, `item_id` matches UUIDv4, exactly 1 VLM request, and `data/wardrobe.db` state (existence/size/mtime) is unchanged. |
| AC2 — `VLM_TEMPERATURE` + `VLM_SERIALIZE_REQUESTS` set → every request carries the configured temperature read fresh per call, and `=true` enables the ING-003 serialized queue | **PASS** | `TestING012_AC2_ConfigClientWiring`. Temperature: attempt 1 forced malformed so the retry is observed too; both requests carry the configured `0.9`, proving the config value reaches the client payload on every call (the CLI uses the documented `func() float64 { return cfg.VLMTemperature }` seam, main.go lines 41/47). Serialization: with `VLM_SERIALIZE_REQUESTS=true` and `VLM_REQUEST_DELAY_MS=500` the stub sees a ≥300 ms gap between requests; with `=false` the gap is <300 ms and the spec's "will not be applied" startup warning is logged — i.e. the delay is genuinely applied only via `WithSerialization`. |
| AC3 — photo whose attempt-1 response fails validation (ING-004) → per-photo retry/flag behavior and `logs/vlm-attempts.jsonl` records are exactly ING-005's, driven through this pipeline | **PASS** | `TestING012_AC3_RetryPolicyDrivenThroughPipeline`: exactly 2 requests, not flagged, emitted fields deep-equal attempt 2's result. Appended log has 2 lines, each with **exactly ING-005's 9 fields**; shared `item_id` equals the emitted `item_id`; attempt 1 `malformed_json` with the unmodified `raw_response` and `photo_path`; attempt 2 clean; logged temperature `0.4` (config default). |
| AC4 — batch in which one photo is flagged for manual review → remaining photos still emit tagged JSON and the flagged photo is reported separately, without aborting the run | **PASS** | `TestING012_AC4_FlaggedPhotoDoesNotAbortBatch`: directory of two photos, first fails both attempts; exit 0, two records, first `flagged=true`, second `flagged=false` and spec-validated, 3 VLM requests, 3 appended log lines, both photo paths correct. The flag is surfaced as a distinct `flagged=true` record rather than an abort. |
| AC5 — VLM unreachable while processing a photo → ING-002 `ErrVLMUnreachable` surfaces distinctly, is not treated as a malformed-output flag, and is not retried into one | **PASS** | `TestING012_AC5_UnreachableSurfacesDistinctly`: connection dropped → exit nonzero, stderr names "unreachable", **exactly 1** request (no retry), stdout empty, **0** appended log lines (not a flag). See also the attempt-2 edge below. |
| AC6 — documented invocation with a photo path or a directory → tagged JSON on stdout and exit 0 on success; nonzero on a hard error (VLM unreachable, missing photo) | **PASS** | `TestING012_AC6_InvocationAndExitCodes`: missing photo → nonzero and stderr names the path; missing `VLM_URL` → nonzero and stderr names `VLM_URL`; directory path → exit 0 with one record per photo (`.jpg` + `.png`). Single-path success and the directory success path are also covered by AC1/AC4. |

**Spec-implied edge cases checked (no failures)**
- **Empty `secondary_colors`**: `TestING012_Edges/empty_secondary_colors...`
  — `[]` is emitted as a JSON array (not null/omitted) and still validates
  against `04-data-schema.md` / `05-vlm-tagging-spec.md`, which require the
  field present with zero-or-more values.
- **Directly-passed uppercase-extension photo** works
  (`TestING012_Edges/a_directly-passed_uppercase-extension_photo...`).
- **Hard error after attempt 1 was already logged** (ticket open point #1):
  `TestING012_Edge_UnreachableOnAttempt2LeavesAttempt1Log` — attempt 2
  dropped → exit nonzero, stderr names both the photo path and the
  unreachable error, exactly 2 requests, empty stdout, and the one appended
  log line is attempt 1's `malformed_json`. This confirms the chosen
  resolution (report path + error, rely on the log for the partial attempt)
  and that an unreachable retry is not converted into a flag.
- **`ItemID` emission** (ticket open point #3): the `item_id` in stdout is a
  UUIDv4 and equals the `item_id` keyed in the attempt log, so the next
  sprint's store row can reuse it and the `later-ideas.md` calibration join
  survives.
- **Attempt-log location** (ticket open point #2): the log lines from the
  CLI land in the repo's `logs/vlm-attempts.jsonl`, confirming the CLI runs
  from the project root with the CWD-relative default path.

**No-new-writes / hard-constraint check (`00-overview.md`, `07-architecture.md`)**
- No catalog row and no `data/wardrobe.db` is created (AC1); `internal/store`
  is untouched.
- No cloud inference or model API: requests go only to the configured
  `VLM_URL` (the stub in tests).
- No new taxonomy/schema/VLM enum values were introduced; emitted output is
  re-validated against `03-taxonomy.md` via the real validator.

**Observations / follow-ups for Planner (non-blocking, no AC failure)**
1. **Stray compiled binary at repo root.** An untracked `ingest` executable
   sits at the project root and is **not covered by `.gitignore`** (which
   ignores `/wardrobe` and `/server` but not `/ingest`). Risk of an
   accidental commit; Coder should delete it and/or add `/ingest` (or a
   broader `cmd` binary rule) to `.gitignore`. Not an acceptance-criterion
   failure.
2. **Directory scan is extension case-sensitive.** `expandPaths` matches
   only lowercase `.jpg/.jpeg/.png/.webp`, so a directory containing
   `PHOTO.JPG` is silently skipped (exit 0, no output), while passing that
   same file directly works. If directory ingestion is expected to handle
   uppercase camera extensions, that is a small follow-up ticket; the
   current ticket does not state case-insensitivity.
3. **"Temperature read fresh per call" is not process-observable.** Once the
   process starts, config cannot change, so the test proves the configured
   value reaches every request but cannot distinguish a fresh read from a
   cached one. The fresh-read property is covered by ING-007's
   `TestING007_AC2_*` tests against the same client API the CLI constructs.

**Pre-existing failure (out of scope, not caused by this ticket)**
- `TestING001_Edge_WhitespaceURLIsAccepted` still fails with
  `config.Load() error = missing required environment variable: VLM_URL`.
  `git diff --stat` shows ING-012 adds only `cmd/ingest/main.go` (untracked)
  and this ticket; `internal/config/config.go` is untouched (whitespace
  trim landed in commit `c356101`). Same pre-existing failure already
  flagged in ING-005/ING-010/ING-011.

## Review

**Decision: APPROVED** — left at `Review`, ready for Ali's final pass and
merge. Reviewer does not set Done.

Reviewed `cmd/ingest/main.go` (untracked, new) and
`tests/ing_012_cli_test.go` (untracked, new) against
`05-vlm-tagging-spec.md` "Input" / "Model" / "Prompt" / "Output
validation"; `04-data-schema.md` (the seven tagging fields);
`06-decisions.md` "Malformed VLM output: retry once at nonzero
temperature, then flag"; `07-architecture.md` "Runtime model", "VLM
request behavior", and "Env var contract"; `00-overview.md`'s hard
constraints; and the ticket's six acceptance criteria. Tester's results
are taken as evidence — this review does not re-run the build/tests.
`git status` confirms no tracked implementation file is modified beyond
this ticket (`backlog/ING-012.md`).

**Spec intent (not just AC wording)**
- This is a *wiring/driving* ticket: it must drive ING-005's processor,
  not reimplement the retry/flag policy. `main.go` calls
  `tagging.NewProcessor(client)` and `processor.Process(ctx, photoPath)`
  and duplicates none of the retry, validation, or attempt-logging
  logic. ✓
- ING-005's API contract is honored exactly: no `ItemID` is minted here
  (`Outcome.ItemID` is used as-is), a `nil` error is not treated as
  success until `outcome.Flagged` is emitted, and a non-nil error is
  surfaced rather than retried or converted into a flag
  (`main.go:64-72`). ✓
- The config→client wiring ING-003/ING-007 deferred is applied:
  `WithTemperature(func() float64 { return cfg.VLMTemperature })` reads
  config fresh per `Tag`, and `WithSerialization(cfg.VLMRequestDelayMS)`
  is added only when `cfg.VLMSerializeRequests` is true
  (`main.go:38-50`). ✓
- ING-005's open integration points are resolved within the options the
  ticket allowed: a hard error after attempt 1 reports path + error and
  relies on the already-written attempt log; the default CWD-relative
  logger lands in the repo's `logs/` when run from the project root; and
  `Outcome.ItemID` is carried into stdout so the attempt-log join
  survives into the next sprint's store row. ✓
- Output validation stays upstream in `ParseTaggingResult`; the CLI
  emits `outcome.Result` without re-deriving or loosening enum values,
  and invents no new `03-taxonomy.md` / `04-data-schema.md` value. ✓

**Hard constraints (`00-overview.md`) — no violations**
- Fully local: only the configured `VLM_URL` is contacted
  (`tagging.NewClient`); `LLM_URL`/`LLM_API_KEY` are read but never
  called. No cloud service or hosted inference path. ✓
- No fine-tuning: base model with the verbatim spec prompt; nothing is
  trained. ✓
- One photo per garment: `Tag` sends one image per request; a directory
  is expanded to one `Process` call per file. ✓

**Standards / non-silent failure**
- No silent swallowing of the documented failure cases: malformed-output
  retry/flag comes from ING-005; `ErrVLMUnreachable` is surfaced
  distinctly and short-circuits with no retry/flag (`main.go:66-69`);
  missing path (`main.go:57-61`) and startup config errors
  (`main.go:25-28`) both log and exit nonzero. ✓
- stdout carries only one JSON object per emitted photo
  (`fmt.Println`); warnings and errors go to stderr via the standard
  logger, so the JSON stream is not polluted (verified by Tester's
  stdout-is-empty assertions on the hard-error paths). ✓
- A non-`ErrVLMUnreachable` hard error also exits nonzero rather than
  becoming a flag, matching ING-005's "hard failure stops the caller"
  contract. ✓

**Non-blocking notes** (do not gate merge)
1. The serialization branch constructs the client twice
   (`main.go:38-50`), discarding the first. Correct, but a single
   `opts` slice would be simpler on a future touch; no behavior impact.
2. A flagged outcome emits the zero `TaggingResult`, so a `flagged:true`
   record carries empty enum strings (`secondary_colors:null`). The flag
   distinguishes it and nothing is persisted, but the next sprint's
   JSON→DB consumer must skip `flagged:true` records rather than insert
   empty enums.
3. `expandPaths` matches only lowercase extensions, so `PHOTO.JPG`
   inside a directory is silently skipped while the same path passed
   directly works. The ticket does not require case-insensitivity; a
   small follow-up if directory ingestion should take uppercase camera
   extensions.
4. The untracked `ingest` binary at the repo root is not covered by
   `.gitignore` (which ignores `/wardrobe` and `/server`, not
   `/ingest`). Coder should delete it and/or add `/ingest` so a build
   artifact is not merged.
5. The ticket's `Entry point` section still reads "needs Ali's
   confirmation"; the CLI was implemented and reviewed, so that text is
   stale documentation rather than a live blocker.

**Merge note for Ali**
Ready for final review and merge. `cmd/ingest/main.go` and
`tests/ing_012_cli_test.go` are currently **untracked** — stage those
(and not the stray `ingest` binary). Among tracked files only this
ticket's backlog file is modified. No protected spec file touched. The
pre-existing `TestING001_Edge_WhitespaceURLIsAccepted` failure is out of
scope and already tracked.

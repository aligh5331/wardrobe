### [ING-026] Automated check that the Tailwind v4 build/config still produces v4 CSS

**As a** wardrobe owner
**I want** an automated check that fails if the Tailwind v4 pipeline
(`@tailwindcss/vite` plugin, single `@import "tailwindcss";`, no v3-era
config files, v4 markers in the built CSS) regresses
**So that** the Tailwind v4 setup `07-architecture.md` mandates is verified by
a command, not by a one-off manual build — the coverage ING-025 retired

**Acceptance criteria** (Gherkin):
```
Given the frontend build pipeline
When `npm test` runs in frontend/
Then the check exercises the real Vite + Tailwind build and fails if the
  emitted `frontend/dist/assets/*.css` stops containing Tailwind-generated
  v4 output (Tailwind custom properties / `@layer` at-rules), rather than
  passing on an empty or unprocessed stylesheet

Given the Tailwind v4 configuration fixed by 07-architecture.md
When the check runs
Then it fails if the `@tailwindcss/vite` plugin is removed from
  `vite.config.js`, if `src/index.css` stops being the single
  `@import "tailwindcss";` entry point, or if a v3-era
  `tailwind.config.js` / `postcss.config.js` is introduced
And those config-shape clauses may be asserted by reading the files — this
  is build configuration, not component behavior, so a structural check is
  the right kind here (unlike the retired ING-020 component suite)

Given the tracked placeholder `frontend/dist/index.html`
When the check runs
Then it does not overwrite that file in place and does not depend on it —
  build output is produced into an isolated/temporary outDir (or the check
  otherwise leaves the tracked placeholder's content untouched)

Given the new check
When `npm test` and `go test ./...` run
Then both are green, and the check adds no runtime dependency (dev/test-time
  tooling only — 00-overview.md's fully-local constraint and the
  single-binary runtime model are untouched)
```

**Spec reference:** `07-architecture.md` "Frontend" (the Tailwind v4 setup:
`@tailwindcss/vite` plugin, single `@import "tailwindcss";`, no
`tailwind.config.js`); `06-decisions.md` "Testing tooling: Vitest + Testing
Library for the frontend, stdlib-first for Go" (tests live under `frontend/`,
run via `npm test`); `backlog/ING-020.md` AC4 (the structural assertions being
restored as an automated check); `backlog/ING-025.md` (retired them and left
this as the recommended follow-up)

**Status:** Review

## Reviewer

**Decision: APPROVED** — passes to Ali for final review and merge.

All acceptance criteria verified against the spec:

- **AC1** — Real Vite build exercised (not mocked). 5 Tailwind v4 markers (`@layer theme`, `@layer base`, `@layer utilities`, `--color-`, `oklch(`) checked via `hasV4Markers()` requiring all 5 — no false positive from partial matches. CSS file existence verified.
- **AC2** — All three structural assertions pass: `vite.config.js` has `@tailwindcss/vite` (verified in file), `src/index.css` is exactly `@import "tailwindcss";` (verified in file), no v3-era config files under `frontend/` (glob confirmed none exist).
- **AC3** — Build output goes to `fs.mkdtemp(tmpdir())`, cleaned in `afterAll`. Tracked placeholder at `frontend/dist/index.html` untouched.
- **AC4** — `npm test` 12/12, `go test ./...` all packages clean.

No hard-constraint violations: no cloud services, no hosted inference, no fine-tuning, no runtime dependency (Vitest is a devDependency). Error handling is correct — `execSync` throws on non-zero exit, no silent swallowing of errors.

Ready for Ali's final review and merge.

## Test results

| Acceptance Criterion | Result | Evidence |
|---|---|---|
| AC1: Real Vite build produces v4 CSS markers | **PASS** | `npm test` ran `npx vite build --outDir <temp>`; all 5 markers (`@layer theme`, `@layer base`, `@layer utilities`, `--color-`, `oklch(`) present in built CSS. 12/12 tests passed, exit 0. |
| AC2: Structural config — `@tailwindcss/vite` plugin present | **PASS** | `frontend/vite.config.js` line 3: `import tailwindcss from '@tailwindcss/vite'`, line 9: `tailwindcss()` in plugins array. |
| AC2: Structural config — `src/index.css` is `@import "tailwindcss";` | **PASS** | File contains exactly `@import "tailwindcss";` (one line, no trailing whitespace). |
| AC2: No v3-era config files under `frontend/` | **PASS** | `glob` for `frontend/tailwind.config.*` → no files found; `glob` for `frontend/postcss.config.*` → no files found. |
| AC3: Build output doesn't overwrite tracked `dist/index.html` | **PASS** | Test uses `fs.mkdtemp(tmpdir())` for temp build dir, cleans in `afterAll`. Tracked placeholder at `frontend/dist/index.html` exists and is untouched. |
| AC4: `npm test` passes | **PASS** | 2 files, 12 tests, 12 passed, 0 failed, exit 0. |
| AC4: `go test ./...` passes | **PASS** | All packages clean: `internal/config ok`, `internal/tagging ok`, `tests ok`, exit 0. |

**Edge cases checked** (no gaps found):
- Empty CSS: `hasV4Markers()` on empty string fails all 5 checks — implicitly covered.
- Build failure: `execSync` throws on non-zero exit — test fails as expected.
- CSS file not found: `expect(cssPath).toBeTruthy()` catches this.
- `--color-` substring: combined with other markers, false positive impossible in practice.
- `src/index.css` exact match: `.trim()` + `toBe()` catches whitespace-only edits or extra lines.
- `vite.config.js` `@tailwindcss/vite` in wrong place: config check is supplementary; the real build test is the actual behavioral check.
- v3-era config in subdirectories: `readdir` checks direct children only, but glob searches confirmed none anywhere.
- Go test staleness: re-ran with `-count=1` — all passed.

**Context:** ING-020 AC4 verified the Tailwind v4 setup *structurally* because
`frontend/` had no test runner. ING-024 added Vitest and ported the component
behavior, but ING-025 retired the structural suite and deliberately left its
AC4 build/config assertions unowned; ING-025's Reviewer and ING-024's Tester
both flagged this as the one Planner follow-up. `TestING021_AC2` neither runs
`npm run build` nor checks CSS markers, and skips on a fresh checkout without
`frontend/dist/assets`, so it does not cover this. This ticket restores the
missing confirmation as one runnable check.

**Routing:** Ready → Coder — it touches `frontend/` config/scripts, and
`agents/tester.md` denies `frontend/src/**`, so the test is Coder-authored
(same split as ING-024/ING-025).

**Out of scope:** changing `App.jsx` or any rendered component behavior;
re-adding a source-grep structural suite for component behavior (that was
correctly retired by ING-025); adding a runtime dependency or a CI service.

**Note:** This is the smallest automated check that closes the gap. A build
run inside the Vitest suite is acceptable at this project's scale; do not
introduce a second test framework or a separate always-skipped command.

## Implementation notes

**Files added**
- `frontend/src/ing_026_tailwind_build.test.jsx` — 4 tests covering all ACs.

**What each test covers**
- **AC1** — Runs `npx vite build --outDir <temp>` into a temp directory
  (never touching `frontend/dist/index.html`), then checks the built CSS for
  Tailwind v4 markers: `@layer theme`, `@layer base`, `@layer utilities`,
  `--color-`, and `oklch(`.
- **AC2** — Three structural tests: (1) `vite.config.js` contains
  `@tailwindcss/vite`, (2) `src/index.css` is exactly `@import "tailwindcss";`,
  (3) no `tailwind.config.js`/`tailwind.config.cjs`/`postcss.config.js`/
  `postcss.config.cjs` under `frontend/`.
- **AC3** — Build output goes to a temp directory created via
  `fs.mkdtemp(tmpdir())` and cleaned up in `afterAll`.
- **AC4** — Both `npm test` (12/12) and `go test ./...` pass.

**Assumptions / decisions**
1. **Vitest `it` timeout.** The build test has a 30s timeout (`it(..., 30000)`)
   because Vite build can take 10–15s; the default 5s is too short.
2. **Temp build output.** Uses `fs.mkdtemp` in `tmpdir()` (OS temp dir) —
   this is a test-only path that is cleaned up after the suite, not persistent
   project scratch space. The Go `fs.ReadDir` on `frontend.Dist` (ING-021 AC2)
   is the only path that reads `frontend/dist/` at test time; this test
   deliberately avoids that to not depend on a previously-built `dist/`.
3. **No mock of the build.** The Vite build is run as a child process —
   not mocked — because the ticket requires the *real* build to be exercised.
4. **Skills check:** reviewed the available skills before writing. No Vitest/
   Testing Library skill exists; `tailwind-v4-shadcn` is Tailwind theming
   setup and is not relevant to these tests. Nothing was applied.

**Local verification (Coder)**
- `npm test` (in `frontend/`) → **2 files, 12 tests, 12 passed, 0 failed**,
  exit 0.
- `go test ./...` → `internal/config ok`, `internal/tagging ok`,
  `wardrobe/tests ok`, exit 0.
- `glob frontend/src/ing_026*` → `frontend/src/ing_026_tailwind_build.test.jsx`
  exists; no `0*.md`/`agents/*.md`/`AGENTS.md` touched.
- Coder did not author or run the Tester acceptance verification — that is
  Tester's role. Status set to `Testing`.
